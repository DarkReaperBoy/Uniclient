# Credentials — never commit
auth/
*.vault

# Go build output & binaries
dist/
*.so
*.dll
*.dylib
*.wasm
*.exe
*.apk
*.apk.idsig
*.idsig

# Web shell generated at build/CI time (never in the repo)
web-build/

# Live API test credentials (env vars, never committed)
.env*

# Nix build artifacts
result
result-dev

# Logs
logs/

# IDE
.idea/
.vscode/
*.swp
uniclient
# Reproducibility lock the owner explicitly dropped (commit dc4ac073);
# regenerates on demand with `nix develop`.
flake.lock
