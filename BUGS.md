# BUGS.md — the open bug list and the fix log

This file is the standing work list from 2026-09-23 on (AGENTS §11
"Bug hunt / stability phase"). Feature work is done — AyuGram parity
closed at 195/200 PRESENT and release **v0.9.1** shipped — so sessions
work from the top of the OPEN list below, plus anything they find on
the way.

The contract (same as everything in AGENTS.md):

- Every fix lands with a **test first** that fails without it.
- Fixed entries move to the Fixed log with their slice/commit.
- Nothing is closed on assumption: the evidence column is what makes
  an entry real, and "measured wrong" corrections get edited in place.
- Finding a new bug while fixing another ⇒ add an open entry even if
  you also fix it right away.

## Audit battery — 2026-09-24 (full sweep complete)

The whole tree was swept with an explicit tool battery before fix work
resumed; every open row below came out of it or from the code each
finding sent us to. Clean results count as "checked" for the areas
they cover:

- `staticcheck 2026.2.1 -checks=all -tags goolm` over all packages:
  326 findings, triaged BY HAND in source — bug-class codes
  (SA4004/SA4010/SA4006/SA9003/SA5008/SA1012/ST1018) each verified
  before logging; style codes (ST1003 naming, ST1000 package
  comments) are not bugs and are not logged.
- `go vet -tags goolm ./...` green; full `go test -race -count=1
  ./...` over ALL 16 packages: **0 failures, 0 data races**.
- TODO/FIXME/XXX/HACK markers in code: **0**. Defer-in-loop scan: 19
  hits, all opened and verified false positives (defer inside `go
  func` closures).
- Cross-builds vs AGENTS' platform table: windows/amd64 `CGO_ENABLED=0`
  OK (documented pure-Go), linux `CGO_ENABLED=0` fails exactly as the
  table documents ("Verified: CGO_ENABLED=0 fails"), android = gogio+
  NDK CI job already exists in verify.yml — no undocumented platform
  regression.
- `govulncheck` (inside `nix develop`, `GOFLAGS=-tags=goolm`; the
  module lives in `go/`, not the repo root): **1 reachable
  vulnerability → B-15**; additionally 1 vuln in required-but-uncalled
  modules and 5 in imported-but-uncalled packages — not reachable, no
  action owed.
- Coverage snapshot (context, not a bug row): cores 8.9%, utils 9.9%,
  gui 18.3%, engine 24.0%; pure-logic packages 71–87%. Repo has ZERO
  `func Fuzz` targets → folded into B-17's next step.
- Two findings had to outrun their tools and the tools were RE-CHECKED
  rather than trusted: the xmpp tag error was reproduced in a
  standalone program (B-11), and the two dead-UI parity rows were
  confirmed with `git log -S` showing the call sites were never wired
  (B-12, B-13) — parity rows corrected with the wrong claims kept
  visible, counts re-derived by the same machine parser.
- `research/*.md` limitation lists were reviewed; per AGENTS they are
  not authoritative, and nothing in them was promoted to a bug row
  without independent code evidence.

## Open (ordered by what a session should look at first)

| # | Where | What | Evidence / how to see it | Next step |
|---|-------|------|--------------------------|-----------|
| B-25 | ts.arcticblaze.net guest text permissions | Guest text permission FLAPS server-side: `TestTeamSpeakLiveRoundTrip`'s server-mode send was ACKed in the morning battery, denied (`permission denied: insufficient client permissions`) at 17:21, ACKed again at 17:26 — possibly flood-control from the investigation's 50+ connections or bot-driven drift. The small-message delivery oracle is therefore opportunistic; the product itself surfaces `ErrPermission` honestly. | battery logs: live_all.log PASS (26.0s) → live_all2.log FAIL at teamspeak_live_test.go:114 (that file's Send-error line moved to :122 when slice 247 added the honest skip) → live_all3.log PASS (27.4s) → still denied at every slice-255/256/257 re-probe, **ACKed again 2026-09-25** in the slice-258 live `-race` run — the flap is ongoing, both directions | RoundTrip now classifies `ErrPermission` as an honest Skip (transport still proven by the structured error arriving over the encrypted channel); re-check opportunistically; owner's own server would restore the oracle |
| B-50 | cores/xmpp.go:7269 (`ParseMessageStyling`) | XMPP messages render with **literal markdown markers** (`*bold*`, `_italic_`) — the styling→span parser exists with zero callers and the GUI only renders `cores.TextEntity` entities (richtext.go), so nothing converts. Readable text (unlike B-51's control garbage) — a polish gap vs styling clients, not corruption. | deadcode (slice-273): `unreachable func: ParseMessageStyling` even with `-test`; gui/richtext grep = zero styling consumers; the parser's own comment lists `*bold*, _italic_, \`code\`, ~strike~` | wire: convert spans → `cores.TextEntity` in the xmpp core's incoming path (tests-first: styled message → entities), or accept literal markers as honest plain text — feature-adjacent, owner's call |
| B-6 | cores/matrix.go:1241 (JoinGroupCall's ErrNotSupported return) | Matrix group calls (MSC3401) not implemented — honest `ErrNotSupported` today. | code + `research/matrix_group_calls.md` (primary-source research: MSC3401 mechanics, mautrix v0.30.0 has ZERO call code — verified by module-cache grep —, what of ours is already reusable, Tier1 full-mesh vs Tier2 SFU/MatrixRTC effort, testing/account needs) | owner decides via the research doc: Tier1, Tier2, or keep status quo |
| B-8 | repo root + `cores/teamspeak.go` `tsQuickLZCompress` | No LICENSE file exists in the repo at all (owner decision pending), and the slice-231 QuickLZ send-side is a byte-port of quicklz.c, whose header says the commercial license "does not cover derived or ported versions created by third parties under GPL". | quicklz.c header (fetched 2026-09-23, RT-Thread mirror); `ls LICENSE*` → none; the reference C is NOT vendored (gcc-built vectors only, like ffmpeg for fixtures) | owner picks the project license and confirms the port stays; until then provenance is documented here — never vendor quicklz.c |

## Fixed — slice 284 (2026-09-25)

| F-54 | **Telegram gzip decompression was uncapped (gzip-bomb OOM).** `gzipDecompress` read output with a bare `io.ReadAll(r)`; BOTH callers are network-controlled (`5685`: decrypted MTProto payload sniffed for the gzip magic; `7148`: V2Reference call signaling) — a hostile ~1 KiB gzip bomb expands to arbitrary RAM. F-53's decompression sibling. | Signaling path OOM from a hostile/compromised server: callers fall back to raw bytes on error (both sites keep working). | RED two ways: **seam** (`undefined: tgGzipMaxDecompressed`) + **behavioral** (bare ReadAll swapped back: `gzip bomb: err = <nil>, want size-exceeded error (out len 1048576)`); GREEN = `TestGzipDecompressCapsBomb` (1 MiB zeros → ~1 KiB input, ceiling overridden to 1 KiB) + `TestGzipDecompressHappyPath` (signaling JSON round-trip); `-race` + whole tree 14 ok. Ceiling 128 MiB, `tgGzipMaxDecompressed` |
| F-55 | **.tgs sticker inflation uncapped.** `ParseTgs` did `io.ReadAll(zr)` on the gzip'd sticker: the download layer bounds the compressed bytes, but a hostile .tgs expands far beyond that before JSON parsing. | Animated-sticker open = memory bomb; cap sits before parse so hostile files fail with a size error instead of inflating. | RED two ways: **seam** (`undefined: tgsMaxDecompressed`) + **behavioral** (`tgs bomb: err = lottie: json: invalid character '\x00'…, want decompressed-size error` — the old path inflated the whole bomb then failed JSON differently); GREEN = `TestParseTgsCapsBomb` (ceiling overridden to 1 KiB) + existing ParseTgs suite; ceiling 32 MiB (legit stickers = a few MB) |
| F-56 | **Bale upload response drained with an unbounded read.** `io.ReadAll(uploadResp.Body)` read a server-controlled body whose result was DISCARDED (drain-for-reuse) — a pure OOM sink with no output benefit. | Hostile Bale server streams an endless upload response → client allocates while swallowing bytes it never uses. | RED: source-scan (comment lines skipped — the first version of the scan matched its OWN explanatory comment, self-caught) reported `bale.go still drains the upload response with unbounded io.ReadAll` + missing wiring; GREEN = `TestBaleUploadDrainIsBounded` (scan) + `TestDrainBodyLimitedStopsAtMax` (infinite body, consumed ≤ ceiling, non-vacuous >0); fix = `drainBodyLimited` via `io.Copy(io.Discard, io.LimitReader(…, 64 KiB))` |

## Fixed — slice 283 (2026-09-25)

| F-53 | **Matrix media download was UNCAPPED — hostile-homeserver OOM.** `DownloadFile` used `m.client.DownloadBytes`; mautrix's own ceiling (`DefaultResponseSizeLimit` 512 MiB) NEVER applies to that path because `DownloadWithParams` sets `DontReadResponse: true` (streamed body, `readResponseBody` skipped) and `DownloadBytes` does a bare `io.ReadAll(resp.Body)` — an arbitrary-length read from a server-controlled body. Every other response in the client is capped at 512 MiB; this one was the hole. | Hostile or compromised homeserver streams an unbounded media body → client allocates until OOM (the F-16/§1.10 DoS doctrine; the only uncapped response path in the whole codebase — engine's own download caps run AFTER the fetch). | RED two ways: **seam** (`undefined: matrixMediaMaxBytes` with only the test present) + **behavioral** (DownloadBytes swapped back in with the cap var present: `oversized body: err = <nil>, want size-limit error` — dest was being written); GREEN = `TestDownloadFileCapsHostileBody` (tiny ceiling, 4 KiB body, asserts error + dest NOT written) + `TestDownloadFileSmallBodyPasses` (byte-exact happy path); `-race` on both, whole tree 14 ok. Fix: `Download` + `io.LimitReader(matrixMediaMaxBytes+1)` → `ErrMediaTooLarge`; ceiling = 512 MiB (matches every other mautrix response, zero behavior change for legit files) |

## Fixed — slice 282 (2026-09-25)

| F-52 | **Vault saved credentials by truncating the file first.** `writeVaultBytes` was plain `os.WriteFile` (O_TRUNC): the old vault bytes are destroyed BEFORE the new ciphertext lands, so disk-full or a crash mid-`Save()` left a truncated/corrupt vault — the only copy of every account credential, session and app config (they all live in the vault). Zero atomic-write helpers existed in utils (no `CreateTemp`/`Rename` anywhere). | Total account loss on any interrupted save: every saved session + config needs full re-auth, and a half-written ciphertext is unreadable by design (no recovery path). | RED: `TestVaultWriteKeepsOldFileWhenWriteFails` quoted `expected the impossible write to report an error — the old os.WriteFile truncated and 'succeeded' silently, destroying the previous vault` (failure injection: read-only temp dir + privileged-run probe); GREEN after the temp-file → fsync → rename rewrite (0600 preserved, best-effort dir sync, zero temp litter); full utils suite + `-race` + whole tree 14 ok; `GOOS=js` vault seam untouched and still builds. Scope triage: drafts = SQLite (journal-atomic), ExportSelfKeys/deltachat key export = user-triggered copies (originals stay in memory), media `os.Create` = re-downloadable cache — vault was THE critical instance |

## Fixed — slice 273 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-51 (= B-51) | **IRC receive path never stripped formatting control codes.** The server relays mIRC codes (bold/underline/strikethrough/invert, `\x03` fg/bg colors, `\x04` hex colors) verbatim; `StripFormatting` existed with ZERO callers and the GUI has no filter — users saw raw `\x02`/`\x03` garbage in chat (the B-14 mojibake class). Wired at all four `Trailing()` extraction points (privmsg — CTCP/ACTION inherit it —, notice, two server-error handlers); the outgoing format helpers were dead since day one → deleted with their const block (deadcode slice-272). | Reading raw control bytes in every IRC conversation: visible garbling exactly like the emoji bug B-14 fixed, on a first-class core. | RED: `TestIRCClientsReceiveFormattingStripped` quoted `PRIVMSG not stripped: "hi\x02!\x0f there…"` + NOTICE; GREEN after wiring; `TestStripFormattingPins` pins the mIRC grammar — **two pins were initially written wrong by me** (single-digit fg IS a color; lone `\x03` is a control byte the stripper rightly removes) and RED caught both — re-check-the-checker; full cores + `-race` green; deadcode 27 → 17 (format fns gone, StripFormatting now LIVE); one Go lesson recorded: no adjacent-string concatenation (`"a""b"` = syntax error, that's C — `\x` takes exactly 2 hex digits so no split was ever needed) |

## Fixed — slice 268 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-50 (= B-49) | **release.yml ran a weaker gate than verify.yml — the two gaps from B-45 existed on the RELEASE path too.** Reading the release pipeline (first full read in slice 267) showed its test job had no live-tag vet and no race legs: a `v*` tag push would build and publish artifacts with less verification than every manual verify run — including code only ever compiled under `-tags live` and the race detector classes (B-37/B-40/B-41) that verify.yml now covers. | Releases are the artifacts owners and users actually run; a tag cut without a local gate silently shipped two unverified classes straight to the GitHub Release. | Steps added verbatim to release.yml's test job; **structural proof**: yq parses the file with the expected step order AND `diff` of the two step bodies against verify.yml's runner-proven originals = **IDENTICAL** (commands were executed green on CI in the B-45 validation run). Honest limit: release.yml cannot be dispatch-validated without PUBLISHING a dev release + gh-pages push (owner-visible side effects) — validation = yq + byte-identical proven commands + the untouched tag-only trigger documented |

## Fixed — slice 267 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-48 (= B-47) | **Eleven pieces of widget/selection STATE were package-level while separate windows render them on their own goroutines.** Separate chat windows run their own event loop (`runSeparateWindow` → own `App`, own goroutine); `Root`'s `frameMu` serializes frames (no torn memory) but the STATE is shared: a call-graph walk from Root's separate branch (1,165 functions) marked exactly 7 of the258 package widgets reachable — `inlineKbdBtns`, `replyKbdBtns`, `musicMenuBtn`, `schedDlgPresets`, `shadowDlgClose/Unban` — plus, by inspection of the same paths, the reply-keyboard session pair (`replyKbdSingleUse/UsedFor`), the seen-inline id and `fwdSel` (worst: ANY window's `openChat`/`openForward` reset wiped ANOTHER window's forward-picker selection). My initial "composer submits go to the wrong chat" suspicion was wrong — those chips are settings-only; the walker corrected the scope (re-check-the-checker). | A press/selection observed in one window can be consumed or reset by another (misrouted bot-keyboard taps, forward selections vanishing mid-pick, single-use reply keyboards consumed twice) — slice 168's rule "each window owns its widgets" simply never reached these eleven. | `TestSeparateWindowWidgetsAreNotPackageLevel` (source-scan oracle, B-14 style): RED listing **12 declarations outside the App struct** (quoted across 6 files) → all eleven migrated onto the per-window `App` (nil-map safe: `openForward` initializes `fwdSel` before any toggle) → GREEN; full gui + `-race ./gui/` + **whole tree 14 ok** |
| F-49 (= B-48) | **The slice-152 inline "Seen" receipt could never render — the wiring line never existed.** `seenInlineGate` requires `isLastOwn`, fed from `m.MsgID == msgFrameLastOwn` — but `msgFrameLastOwn` had **zero assignments** anywhere, and the pure helper `lastOwnMsgID` (which computes exactly the frame's last own message, WITH its own passing unit tests) had **zero production callers**: pure layers tested green around a wire that was never drawn (the B-12/B-13 dead-parity class, third instance). | The DM "Seen HH:MM" row under the last own message — a shipped, documented feature — was dead code; users never saw receipts. | `TestSeenInlineReceiptIsWired` RED (both the missing `messageList` assignment and the non-App read quoted) → wired: `a.msgFrameLastOwn = lastOwnMsgID(f.messages)` once per `messageList` pass, read App-scoped (same pass, same window — correct under multi-window too); GREEN. Rendering itself is headless-unverifiable — the pin is structural + the gate's existing pure tests (documented honestly) |

## Fixed — slice 266 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-46 (= B-45) | **CI never verified two things every local gate runs.** First full read of `verify.yml` (dispatch-only, per AGENTS §1.5): the **`live`-tagged integration tests were never compiled on CI** (a broken `tests/*_live_test.go` passed every green run — only local `vet -tags goolm,live` caught breakage) and **CI ran no race detector at all** (B-37/B-40/B-41-class races were only ever caught by manual local gates). | The gate of record must enforce what the documented gate enforces — otherwise a push without a local gate silently drops two whole verification classes. | Workflow edit = the artifact; RED evidence = the workflow content vs the local-gate diff (steps absent, quoted); both new commands run **verbatim locally first** (`livevet-ok`, both race legs `ok`); **end-to-end GREEN: the post-push dispatch executes the new steps on the runner** (verified in the run for this commit). YAML structure machine-checked with yq (a `//` typo in a comment was caught before push — GitHub would have rejected the whole workflow) |
| F-47 (= B-46) | **`TestCachedStarGifts` fails on every in-process repetition: package-level cache vs per-run cold assumption.** The star-gift catalog cache (`gifts.byAcct`, 10min TTL) lives at PACKAGE scope — it survives the test's fresh Engine, so `-count=3` failed with `fetches = 0, want 1` on runs 2 and 3 (run 1 green: the pattern that proves cross-run state). Production caching is intentional (design intact); the test's "cold fetch" assumption was the broken part. | First flake found by the `-count=3` hunt (shuffle was already green) — an oracle wrong on repetition is exactly the F-9 class: the test lied about the second run. | RED captured live: `gifts_test.go:60: fetches = 0, want 1` ×2 from the count3 run (quoted); test-side fix: reset `gifts.byAcct` at start + `t.Cleanup` (F-9 discipline — fix the oracle, not intentional production caching); GREEN: gift test ×3, **full suite `-count=3`: rc=0, 14 packages ok** |

## Fixed — slice 265 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-45 (= B-44) | **Per-frame log flood in the video-note loop.** `videoAudioLoop` is called from `drawVideoNoteFrame` — the draw path that re-invalidates every frame — and its `SeekMedia` failure branch logged unthrottled: a persistently failing seek (dead audio device, broken player state while the picture keeps looping) printed ~60 lines/second for as long as the note played. Third instance of the flood doctrine after B-42 (stdout) and B-43 (gap log). | Unbounded log writes from the UI hot path drag the exact frames they run on, and drown every other diagnostic — the failure mode this whole phase exists to prevent. | Emission decision extracted as the PURE `loopLogAllowed(last *time.Time, now, minGap)` — **injected clock, deterministic at every wall time (F-9 discipline)** — wired behind a per-App mutex + 10s gap. RED (build): `undefined: loopLogAllowed` (seam-RED quoted); GREEN: `TestLoopLogAllowed` pins first-allowed / suppressed-inside-gap / boundary-allowed / re-suppressed + "suppressed checks don't move the timestamp"; full gui suite + `-race` green. Call-path flood itself not unit-constructible (MediaState gates on the live engine) — the per-frame call site + pure throttle are the documented evidence pair (F-15 precedent for seam-RED) |

## Fixed — slice 264 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-44 (= B-43) | **Command-queue gap diagnostic logged once PER PACKET instead of once per gap.** The B-19 fix (surface queued out-of-order ids instead of dropping them) re-entered on every packet arriving while one gap was open — the slice-247 live storms already showed 229 identical lines in a single run, and a hostile server could deliberately hold one in-order packet hostage to flood stderr (same doctrine as B-42's stdout fix, one slice later). | Unbounded stderr writes are a DoS/perf vector on an unbuffered log, and the diagnostic's VALUE is one line per gap — repeats only bury it. | RED: `TestTSCommandQueueLogsGapOncePerGap` drove 50 calls during ONE open gap — **50 identical lines** captured (quoted in the failure); fix = `gapLogged`/`gapLoggedFor` state on `tsConnection` (guarded by the existing `recvQueueMu`, reset when the gap resolves): log only when the expected id changes. GREEN: exactly 1 line for the gap, 2 after resolving + reopening a NEW gap (pins once-PER-gap, not once-ever); B-19's original content-assertion test stays green untouched; full cores + `-race` green |

## Fixed — slice 263 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-43 (= B-42) | **TeamSpeak receive path printed unconditionally to stdout — hostile-packet flood vector.** Eight packet-triggered diagnostics (`cmd/fake decrypt FAIL`, `ack decrypt FAIL`, `VOICE decrypt fail/no crypto`, `FRAG/RX decompress FAIL`, `cmdCh FULL`) went to stdout with no gate: every garbage or mis-keyed packet produced a line (unbuffered write per packet = syscall flood; a GUI app has no console to absorb it anyway). The reconnect-attempt print was also un-timestamped stdout. | A hostile server (or a plain key mismatch) could turn the client's stderr/stdout into a per-packet flood — the same class as every other RX-path hardening this phase; the file's own convention is the `tsLogf` (UNICLIENT_TS3_DEBUG) gate. | RED: `TestTSReceivePathSilentOnBadPackets` drove 150 framing-valid but undecryptable command/ack/voice packets through the REAL `tsHandlePacket` with stdout piped — **5,700 bytes captured** (flood quoted: `[TS3 VOICE] no crypto…` ×50); GREEN after routing all 8 through `tsLogf` + reconnect through `log.Printf`: 0 bytes, only tsLogf's own gated implementation remains; full cores suite + `-race` + live TS Handshake/LargeMessage PASS post-conversion |

## Fixed — slice 259 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-42 (= B-41) | **Repo-wide lock-order cycle: DeltaChatCore `mu ↔ transportMu`.** `AddTransport` holds `mu` (defer, whole function) and acquires `transportMu`; `SelectAccount` held `transportMu` (defer) and acquired `mu` — opposite orders = a pure two-party deadlock (concurrent account-switch + transport-add freezes BOTH goroutines forever, no third lock needed). Found by extending slice-258's GetMembers discovery into a full-repo order audit (33 ordered pairs across 638 files; this was the ONLY cycle — the rest of the tree is consistent). | A frozen core is unrecoverable without restart — account switching is a UI action, AddTransport runs on setup, so the pair is reachable in normal multi-account use. | `TestLockOrderIsAcyclic` (NEW, permanent): parses every non-test .go under `go/`, tracks held locks per function with **defer-awareness** (defer-Unlock holds to function end — that subtlety hid a real pair from the first draft), fails on any A↔B cycle naming both directions. RED quoted: `scanned 638 files, 33 ordered edges` + `cycle: transportMu <-> mu — SelectAccount@d / AddTransport@d` → `SelectAccount` fixed with the snapshot pattern (entry under `transportMu`, identity update under `mu` alone, canonical order documented at the field decl) → GREEN at 32 edges. **Self-catch**: my Go port of the audit helper had an off-by-one (slicing from the `(` instead of past it) that made the test PASS vacuously with 0 edges — caught by diffing edge counts against the throwaway script (0 vs 33) and a probe test; the helper now logs the edge count every run so silence can't masquerade as clean. Full cores/engine/gui suites + `-race` green |

## Fixed — slice 258 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-41 (= B-40) | **Initserver identity swap raced the live receive loop.** `tsHandshakeRetry` wrote `tc.clientID` (from `initserver aclid`) and `t.myClientID`/`t.myName` with NO synchronization while the receive goroutine — started earlier in the handshake — read them on EVERY packet (`tsSendAck` meta/packet build, `tsSendPong`) and in receive-path handlers (text self-echo check). Found by the first-ever **live battery under `-race`** (5 reports). The same audit caught a **pre-existing deadlock pair**: `GetMembers` acquired `clientInfoMu` while holding `t.mu` (defer), while `GetGroupCall` holds `clientInfoMu` and calls `selfMuted()` → `t.mu` — opposite orders deadlock the moment any `t.mu` writer (e.g. SetNickname) appears; plus the slice-253 SendVoice talk-power guard had the same order violation, and GetSessions/TerminateSession/JoinChannel needed self-info snapshots hoisted above `t.mu`. | A half-read client id builds malformed ACK/PONG packets mid-handshake (the server drops the connection under unlucky timing — un-instrumented runs just lose that race most of the time); the deadlock pair could freeze a live voice session (the call bar polls GetGroupCall every second while SetNickname takes t.mu). | TWO race tests RED pre-fix (live quotes: `WARNING: DATA RACE` at `teamspeak_identity_race_test.go:33/37/38` and `:56/60` — the exact production write/read pairs) → GREEN post-fix: `tc.clientID` → `atomic.Uint32` (13 read sites → `uint16(Load)`, write → `Store`); self-info family → `clientInfoMu` both sides (B-37's canonical guard; field comment documents `myUID`'s program-order exception); snapshots hoisted BEFORE `t.mu` in SendMessage/UploadFile/GetSessions/TerminateSession/JoinChannel/SendVoice; `GetMembers` reordered `clientInfoMu→t.mu`; defer-aware lock-order audit over all 19 `clientInfoMu` acquisitions → **0 violations**; **live TS suite under `-race`: 5 warnings → 0, rc=0 (3 PASS + 1 honest SKIP)** |

## Fixed — slice 254 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-39 (= B-38) | **webm EBML parent-boundary family: payload starting past its element's end.** `readID`/`readSize` can advance the parser past the parent element's `end`; every call site clamped only `e` and never checked `s > e`. Fuzz found it live: `checkDocType` panicked `slice bounds out of range [8:7]` (crasher reached through vp9anim's Parse). The identical pattern sat at 10 sites — raw `p.data[s:e]` slices (checkDocType, codec id, BlockAdditional) plus `q.pos = e` backward steps that could spin a loop. | Malformed or hostile media must never panic the client (fuzz contract, §1.10): a crafted sticker or video file crashing the whole app is a denial-of-service; the backward-pos variant was an unhit infinite-loop hazard. | `s > e` guards at all 10 payloadRange sites (segment loop, checkDocType → false, parseInfo, parseTracks, parseTrackEntry → ErrBadElement, video w/h, parseCluster, parseBlockGroup, BlockAdditions inner, parseBlockMore); regression = saved crasher `vp9anim/testdata/fuzz/FuzzParse/e454d02053346293` replayed by every `go test` (RED with the panic, GREEN post-fix); fuzz 15s ×2 post-fix: 1,750,649 + 65,277 execs, zero new crashers |
| F-40 (= B-39) | **webm parseTrackEntry raw codec slice panic [36:35].** Inside a track entry whose children overran the entry boundary, `codec = string(p.data[s:e])` sliced with s past e (crasher `9470e433aa9dcc92`) — the same mechanism as F-39 but in the track parser that B-17 had just hardened for read errors. | Same never-panic contract; this one sits on the video-note/sticker decode path, so a hostile file would kill the client at open time instead of failing the one clip. | `s > e` → `return 0, ErrBadElement` (B-17 doctrine: hard error, never fabricated track state); regression = crasher `webm/testdata/fuzz/FuzzParse/9470e433aa9dcc92` replayed every `go test` (RED `slice bounds out of range [36:35]` at webm.go:415 pre-fix — the codec slice now sits at :436 behind the guard, GREEN post-fix); both fuzz targets then clean for 15s each with 1.75M/65k execs |

## Fixed — slice 253 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-37 (= B-26) | **Talk power: voice fired blindly into rooms the server silently discards it in.** The core knew neither its own `client_talk_power` (the server SENDS it in `notifyclientupdated`, observed live, previously dropped after a nickname update) nor a channel's `channel_needed_talk_power` (`channellist` doesn't carry it — nobody ever fetched `channelinfo`), so `SendVoice` was pure fire-and-forget into gated rooms: user talks, audio dies server-side, UI shows a healthy room (§1.10-class). | Wired end-to-end: capture own power from `notifyclientupdated`; async `channelinfo` refresh after EVERY successful join (fail-open until it lands); `SendVoice` refuses known-blocked rooms with `ErrPermission` (never a false mic); `GetGroupCall` reports `Meta[talk_power] = blocked/ok/unknown`; engine maps it to `GroupCallInfo.TalkPowerBlocked`; the polled call-bar subtitle says **`mic blocked (talk power)`** instead of a participant count. | RED battery, all staged so every behavior failed pre-wiring: capture test (`myTalkPower = 0`), refresh test (`never populated after join … nobody fetched`), guard test (`returned <nil> … a voice packet was still sent`) + fail-open/sufficient pins GREEN, core Meta test (all three states: blocked/ok/unknown), engine mapping test, GUI subtitle test (`"voice room · 2 participants"` — must surface the block) with unchanged-wording pins; post-patch full cores + engine + gui + `-race` green; live TS set: Handshake PASS, LargeMessage PASS, 2 honest environment SKIPs |
| F-38 (= B-37) | **Data race on `myChannelID`** (found while wiring B-26). `tsHandleClientMoved` wrote `t.myChannelID` with NO mutex; `GetGroupCall`'s self-add read it BARE (after the clientInfoMu.RUnlock — the participants loop's lock does NOT cover it), `tsHandleTextMessage` read it bare, and only one site was consistently locked. | Fixed: the write merged INTO the existing clientInfoMu lock block (no second lock — same mutex), the self-add read wrapped (decide under lock, append outside), the text-message read wrapped. | RED: `TestMyChannelIDConcurrentMoveAndReadRace` (2000 concurrent moves vs GetGroupCall reads) — `-race` reported `WARNING: DATA RACE` pre-fix at teamspeak.go:3143 vs :4760, FAIL (line numbers at RED time — the write now sits at :3168 after the slice-263 shifts, the read at the `selfInThisRoom` snapshot); post-fix `-race` clean; full suites green |

## Fixed — slice 252 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-36 (= B-23) | **Five dead notification registrations per connection.** The handshake sent `servernotifyregister`×5 and the live server answered EVERY one with `error id=516 invalid client type` — a ServerQuery-only command (the server's own message names the offending client type). The exported `ServerNotifyRegister`/`ServerNotifyUnregister` pair had ZERO callers anywhere in the repo and could only reproduce the same 516. Removed: the five handshake calls plus the two dead functions (evidence comment preserved at both sites). | 5 wasted round-trips + 5 swallowed errors on every connect, and a misleading public API that could never succeed for an in-client connection; events were already proven native without any registration — every handler class observed across the batteries (clientmoved/enterview, text+poke, channel events, connectioninforequest→setconnectioninfo auto-respond) | pure deletion → no RED possible (F-26 precedent): compiler (`go vet` rc=0, `servernotifyregister` executable count → 0) + full unit suite + LIVE subset post-removal: Handshake PASS, **LargeMessage PASS — the second client RECEIVED the text, which is the `notifytext` flow itself, delivered natively with zero registrations**; RoundTrip honest SKIP (B-25 permission) |

## Fixed — slice 251 (2026-09-25)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-34 (= B-24) | **tsExec's void deadline returned SUCCESS on pure silence.** `case <-deadline:` built a result from EMPTY dataLines and returned nil — a lost UDP response became a FALSE ACK for state-changing commands (a `clientmove` that "joined" a channel the server never moved us into). Now: nothing at all after `tsExecVoidTimeout` → ErrNetwork-family error; data-without-terminal-error keeps the 500ms grace success (real servers do omit the final ok). | Timeout set to 5s after live evidence: this server's in-order queue reordering occasionally delays responses past 2s (first live run post-fix showed exactly that). The row's premise HELD — even the apparently-unanswered `sendtext` had its `error id=2568` on the wire (see F-35 for where it went). | seam var `tsExecVoidTimeout` (shrinkable in tests); RED: `TestTSExecSilentServerIsAnErrorNotFalseAck` failed pre-fix with `silent server returned SUCCESS (rows=[]) — false ack (B-24)`; controls `TestTSExecConsumesDataThenOk` + `TestTSExecMapsErrorResponse` were GREEN pre-fix (fix touches only pure silence); real-UDP harness (concrete `*net.UDPConn` can't be faked) + scripted dispatcher |
| F-35 (= B-35) | **Concurrent tsExec callers stole each other's responses.** tsExec stored its wait channel in the SINGLE shared `tc.execCh` field: a second caller (SetConnectionInfo spawned by `go` from the `notifyconnectioninforequest` handler — the server fires these in bursts — while `sendtext` was in flight) replaced the channel under the first, so the first command's response was consumed by the wrong waiter and the first starved into a timeout. | Surfaced because F-34's stricter timeout turned this long-masked cross-talk into visible live failures; the RoundTrip debug trace caught the proof: `TX sendtextmessage` → `notifyconnectioninforequest` → SetConnectionInfo overlap → `RX error id=2568 … failed_permid=218` on the wire while sendtext's caller reported "no response within 5s". Fix: `execSeq` mutex serializes WHOLE exec sessions per connection (TS3 answers in order — serial sessions pair perfectly, each bounded by the void timeout). | RED: `TestTSExecConcurrentCallsKeepTheirOwnResponses` (two goroutines + scripted in-order responses) failed pre-fix with `command-one: no response … (response lost)` — the exact live signature, deterministic; green post-fix with both callers keeping their own rows; `-race` clean on all four exec tests; **live RoundTrip flipped 3/3 false-FAIL → 3/3 honest SKIP** (B-25's permission drift now surfaces reliably as ErrPermission instead of being lost to cross-talk) |

## Fixed — slice 249 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-31 (= B-29) | **DeltaChat header keys were case-sensitive in a case-insensitive protocol.** `parseRawHeaders` preserved whatever case the sender wrote while readers looked up a MIX: `content-type` was dual-checked (the author KNEW case varies) but `Chat-Edit`, `Chat-Delete`, `Chat-Group-Member-Added/Removed`, `Autocrypt` and `Content-Disposition` had NO lowercase fallback | an RFC 5322-legal lowercase sender would have edits/deletes/member-events silently skipped, attachments dropped and Autocrypt ignored. Fix: keys stored `strings.ToLower(TrimSpace(…))` at construction; `decryptPGPMIME`'s inner-header list now stores lowercase too (otherwise the outer/inner merge at line4360 would keep BOTH spellings and the documented "inner takes precedence" contract would silently break); all ~17 reads lowered; the redundant dual-case fallbacks collapsed. | RED: `TestParseRawHeadersNormalizesKeys` (input `Chat-Version`/`CONTENT-DISPOSITION`/`autocrypt` → keys must come out lowercase; pre-fix output `map[CONTENT-DISPOSITION:… Chat-Version:1.2 …]`); value semantics, trailing-no-newline and empty-input pins written alongside; **the full IMAP/SMTP localserver chain test stayed green = the canonical-case path didn't regress**; reader audit by grep (zero mixed-case lookups left in the function range) |
| F-32 (= B-30) | **Folded continuation lines joined with double spaces.** The fold join added an unconditional space AND kept the value's pre-fold trailing space, so `Chat-Group-Name: The \r\n\tGroup Name` produced `The  Group Name` in group titles/subjects. | Fix: add the separating space only when the value doesn't already end in whitespace. | RED: `TestParseRawHeadersValueSemantics` folded-continuation assertion failed pre-fix with `"The  Group Name"` (caught by the B-29 test suite while writing it — own finding, logged as its own row per contract); green post-fix, all header tests + full cores suite + `-race` clean |
| F-33 (= B-31) | **Autocrypt keydata corrupted by header folding → SILENT E2EE downgrade.** OpenPGP keydata is hundreds of base64 chars, so every compliant mailer folds the header at 76; the fold join injected spaces into the base64 stream; `base64.StdEncoding.DecodeString` rejected it; the error was swallowed (no else-branch — deltachat.go had ZERO logging anywhere) → the peer's public key silently never stored, `entity` nil, outgoing mail can never encrypt to that peer | while everything else about the peer (name, prefer-encrypt) looked perfectly healthy. Same function serves Autocrypt-Gossip (group key distribution). The chain test's Autocrypt check was a log-only note (never failed), which is why this survived. Fix: strip ALL whitespace from keydata before decoding (spec-compliant unfolders may legitimately leave WSP) + log decode, keyring-parse and empty-entity failures via std `log` (repo convention). | RED: `TestAutocryptKeydataSurvivesHeaderFolding` — generates a REAL OpenPGP key, base64-wraps it into 70-char folds, runs it through the REAL parser into `updateAutocryptPeer`; pre-fix failed with `folded keydata never stored (B-31): decode failed silently — PublicKey=[] … entity=<nil>` while DisplayName/PreferEncrypt were set (exact silent signature); green post-fix asserts stored bytes == serialized key + entity parsed; full cores suite incl. localserver chain + `-race` green |

## Fixed — slice 248 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-29 (= B-27) | **Matrix core could crash on a malformed state event.** `applyStateEvent`'s `StateMember` branch dereferenced `*evt.StateKey` with no nil guard while BOTH sibling paths (`handleMemberEvent`, the sync room loop) check it first — inconsistent and exploitable: one state event without `state_key` from a malicious/buggy homeserver = nil-pointer panic = the whole client dies. Guard added mirroring the siblings. | first-ever Matrix test suite (slice 248 closed the ZERO-tests gap — 19 tests over `eventToMessage`/`roomToDialog`/`generateRoomName`/`isSpace`/`applyStateEvent`/`handleMessageEvent`/`GetDialogs`/`getPinnedEvents`); the whole matrix area was untested, which is exactly how this asymmetry survived | RED: `TestMatrixApplyStateEventNilStateKeyDoesNotPanic` (scripted StateMember with `StateKey=nil`) panicked pre-patch with `invalid memory address or nil pointer dereference`; green + `-race` post-patch |
| F-30 (= B-28) | **Byte-slices on UTF-8 text across four cores = mojibake.** Six preview/title cutters sliced by BYTES — `matrix.go` thread titles (`title[:50]+"..."`), `github.go` first-line quote previews (`[:100]`), the shared `truncate()` helper behind every GitHub API-error body, `irc.go` quote previews (`[:80]`), `deltachat.go` reply previews (`[:100]`) and forward subject lines (`subj[:50]`) | so any multi-byte rune straddling the cut produced INVALID UTF-8 (`"日\xe6..."`, `"😀\xf0..."`), and the length TRIGGER was byte-based too (a 3-rune `"ééé"` = 6 bytes got cut even though it was under the character limit). Fixed by new `utils.Truncate` (rune cut, no suffix) + `utils.TruncateEllipsis` (rune cut + "..." only when a cut happened), swapped in at all 6 sites; `github.truncate` now delegates. | RED: `TestTruncateHelperKeepsUTF8Valid` failed pre-fix on 4/5 inputs (invalid UTF-8 listed byte-by-byte, incl. the trigger bug); `TestTruncateHelperSemantics` (ASCII `hello world`→`hello...`) was green pre-fix proving the ASCII contract didn't move; utils seam itself was build-RED first (tests written before `truncate.go` existed, F-15/F-23 precedent); `utils/truncate_test.go` pins 8 ellipsis + 5 plain cases incl. 4-byte emoji at the exact boundary |

## Fixed — slice 247 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-28 (= B-22) | **TeamSpeak voice live-test flake — root-caused COMPLETELY to server environment (two mechanisms) and hardened.** All three observed failure modes explained: **(1) talk-power gating** — channels with `channel_needed_talk_power` > the guest's `client_talk_power` (20) have their voice SILENTLY discarded server-side: every "both in room yet zero relayed" failure was a gated room (needed 9999/9999/125), every passing room needed 0; **(2) auto-creator relocation** — the `◊ AUTO CHANNEL CREATOR ◊` channel (cid 42, needed=999999) runs a "PrivateChannelManager" bot that creates `<nick>'s Channel` and moves each joiner into it (RX trace: `notifychannelcreated cid=1141 … channel_topic=Created by PrivateChannelManager` + `notifyclientmoved`), splitting the pair (whoami: A@1141 B@1142 vs requested ch:42) — this also produced the original "room holds 1 clients" mode. | probe + RX plaintext evidence table (rooms 43/47/52/42 vs 70/94); voice TX byte-identical pass vs fail (pIDs 0..74, same sizes) and B's RX voice count = 0 at socket level (rules out decrypt); pass/fail parity of packet storms (rules out the queue-gap log) | `TestTeamSpeakLiveVoiceRoundTrip` hardened: memoized power filter (`channelinfo` ≤ own talk power from `clientinfo` — rejects gated rooms AND the auto-creator in one predicate), physical `WhoAmI` verification before streaming + one rejoin + honest Skip if the server insists on splitting us, cache listing downgraded to warning (the voice round-trip is the real proof), no-joinable-channel and asymmetric-perms → Skip with evidence instead of Fatalf. Kept permanent: `tsRedactCmd` (password-redacted debug command log) + TX/RX plaintext command logs, all gated by `UNICLIENT_TS3_DEBUG`. **Validation: 4× live runs rc=0 (1 PASS 75/75 voice + 3 documented environment SKIPs), then the FULL live battery 15 PASS / 0 FAIL / 2 SKIP, live_rc=0** |

## Fixed — slice 246 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-27 (= B-5) | **Inline play failure consumed the marker silently.** `startVideoNoteInline` passed NO onFail, so a permanently-unplayable note completing via the play-on-done marker showed nothing until the next tap; and `ensureH264Player`'s `p.path==path && p.failed` early-return swallowed onFail even when a caller DID pass one — the entry is known-bad and will never play, so the fallback had to run there too. | Both fixed: the completion entry now hands off exactly like the viewer path (toast "Can't play this video in-app — opening system player" + `openMedia` → system player, §1.10 no dead bubble), and the failed-entry early-return notifies. | RED: `TestInlinePlayFailureHandsOffToSystemPlayer` (garbage .mp4 through the real setPlayOnDone → onDownloadComplete → parse-fail path with `openExternalAsync` stubbed) failed pre-patch with `opened=[] … (B-5)`; second part pins the already-failed early-return (onFail must still fire). **-race caught a data race in the first draft of this very test** (unsynchronized stub slice + flag vs the parse goroutine) — fixed with a mutex in the harness before landing; post-patch both tests + full gui suite + `-race` green |

## Fixed — slice 245 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-26 (= B-18) | **Dead-code cluster triaged wire-or-delete.** 58 symbols removed across gui/engine/cores (staticcheck U1000 **128 → 70**). Two premises inside the original row were WRONG and are corrected here: (a) "call rating never opens" — it is LIVE: `onCallStateEvent` arms the dialog INLINE (callui.go) and app.go renders it; the flagged openers were superseded duplicates — the inline block was consolidated into `openRateCall` (behavior-identical) and `maybeRateCall` deleted; (b) `slowmodeSendBlocked`/`muteDlgCustomErr` deletion needed MORE than the declaration: every remaining reference was a pure WRITE (unused's read-based analysis was right) — write sites removed too; while doing so it surfaced that muteDlgCustomErr's validation hint (`Use a duration like…`) had NO display path ever — dead state from an unbuilt hint, part of this cluster not a separate bug. | KEPT with evidence: `audio/opensl_layout.go` (U1000 is a linux-view artifact — `opensl_android.go:266+` consumes locatorType/pLocator/deviceType under the android tag) and the mumble + TeamSpeak protocol enum tables (reference tables, deliberate). Also deleted: abandoned overlay/tile/field leftovers (cornerReplyOverlay, closeInstantView superseded by locked inline closes, openTtlDialog duplicate of openTtlDialogNow, layoutOneTimeChip, stopWebmEmojiPlayer superseded by the inlined eviction stop, the telegram tgcalls-era SDP experiments, …). | Pure deletion has no RED possible (documented like F-22): the machine evidence is the COMPILER (every deletion survived `go vet -tags goolm ./...` rc=0 — a missed reference fails the build) + the full test suite (all 14 packages ok) + staticcheck recount 128→70 with no new findings + gofmt clean; the callrate consolidation is behavior-identical (same state fields, own lock) and review-pinned |

## Fixed — slice 244 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-25 (= B-17) | **Media parsers swallowed read errors + the repo had zero fuzz targets.** `parseTrackEntry` did `num, _ = q.readUint(...)`: a malformed TrackNumber/TrackType/DefaultDuration became a fabricated 0 that the track gate reads as "absent" — malformed input indistinguishable from a file with no video track (§1.10). Now those decision-critical fields return `ErrBadElement` (signature `(uint64, error)`, propagated through `parseTracks`), while two lenient paths were KEPT deliberately with the reasoning in-code: `parseBlockMore`'s add-id (optional alpha — malformed must degrade to "absent", never fabricated bytes) and the optional w/h `if ok` style (absence, not swallowed failure). vp9anim's `colorRange` bit-read was made explicit too (provably recovered by the next frame-size read, but the branch costs nothing). | Fuzz targets added: `webm.FuzzParse` + `vp9anim.FuzzParse` with seeds from the builders and the committed official streams — contract: never panic, never (nil, nil). | RED: `TestMalformedTrackNumberIsAnErrorNotAnAbsentTrack` failed pre-patch (`err=webm: no VP9 video track` — malformed read as absent, want ErrBadElement); green post-patch; **750k+ fuzz execs across both targets (648,852 webm + 99,145 vp9anim + post-patch re-runs) crashed ZERO times**; full webm/vp9anim suites green; seed-corpus mode (what CI runs) is instant |

## Fixed — slice 243 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-21 (= B-19) | **Discarded gap diagnostic.** The out-of-order branch of `tsProcessCommandQueue` built `keys` from recvQueue and dropped the slice on an empty line (SA4010): a wasted allocation on every command-queue gap and the intended queued-packet logging never happened. | Now logs `ts: waiting for command packet %d; queued out-of-order: %v` (still under the documented recvQueueMu contract; `log` import added — teamspeak.go previously only had fmt debug prints). | RED: `TestTSCommandQueueLogsGapWithQueuedIDs` (crafted pktState=5 with queued 7,9 through the real `tsProcessCommandQueue`) failed pre-patch with `gap log … got ""`; green post-patch (log names 5 and lists 7, 9) |
| F-22 (= B-20) | **Test assertions that could not fail.** `drawer_test`'s account-scoping branch had an EMPTY body (SA4006+SA9003 — the case proved nothing) and `stickers_test` reassigned `sel` in the no-recent case without checking it. Both now assert the real contract: acc2/100 must resolve to ITS OWN DM title, and an in-range selection survives `stickerTabs`. Production was CORRECT in both places (contactChat compares AccountID at contacts.go:140-147; stickerTabs keeps in-range sel at stickers.go:52-54 — both source-verified), | so no RED exists for correct behavior — the assertions themselves are the fix and WILL fail if scoping or clamping regresses. | new assertions green against the verified-correct production code; the rows that flagged them were staticcheck SA4006/SA9003 on the test files |
| F-23 (= B-21) | **Nil-context landmine at first login.** `finalizeAuth` passed `syncAccount(nil, …)` (SA1012). Safe only while nothing dereferences ctx — one future `ctx.Done()`/RPC use turns first login into a panic. | Now: `syncAccountContext` replaces nil with `context.Background()` at the boundary (the documented contract) AND the call site passes Background explicitly (defense in depth). | RED: `TestSyncAccountContextNeverNil` failed to build pre-patch (seam undefined); green post-patch pins nil→Background plus pass-through of a real ctx |
| F-24 (= B-7) | **Silent auto-download errors.** `maybeAutoDownload` discarded `RequestDownload`'s result: correct-for-ErrStreamActive (playback is already saving the bytes) but every other error — missing media row, uninitialized manager — vanished, so a broken pipeline was indistinguishable from "settings said no". | Now logs `[engine] auto-download …: err` for everything except ErrStreamActive (RequestDownload never returns ErrStreamBusy — its return paths are not-found / stream-active / media-not-init / enqueue, so Active is the only benign exception; `errors`+`log` imports added to events.go). | RED: `TestAutoDownloadLogsUnexpectedRequestErrors` failed pre-patch (`… was silent; log = ""`), green post-patch; `TestAutoDownloadStaysQuietOnStreamActive` guards the benign exception (claims the stream via the real guard, asserts an empty log) |

## Fixed — slice 242 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-20 (= B-16) | **TeamSpeak server name never stored or surfaced.** The `initserver` handler's store branch was an empty `if` with a "Store server name etc" comment (SA9003), `virtualserver_name` was read nowhere else in the repo (2 occurrences total: this empty read + the servercreate WRITE), `ServerInfo()` had zero callers — and `GetDialogs` hardcoded `Title: "Server Chat"`. So a server's own name never reached the chat list and a live rename was invisible. Now: `initserver` stores it (`setServerName`, own mutex — GetDialogs reads it under `t.mu.RLock`, no lock-order inversion) and the server dialog's title uses it, falling back to the historical label when no name has arrived. | The serverinfo-row store from the row's next step was NOT added: `ServerInfo()` has no production callers (grep-verified), so it would have been dead code — initserver is the standard TS3 carrier of the name. | RED: `TestTeamSpeakInitserverStoresAndSurfacesServerName` (uses only pre-existing API — `tsHandleServerCommand` + `GetDialogs`) failed pre-patch with `title = "Server Chat", want the virtualserver_name… (B-16)` for both the initial push and a rename push; `TestTeamSpeakServerDialogFallsBackWithoutName` guards the no-name fallback; green post-patch, full cores suite + `-race` clean |

## Fixed — slice 241 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-19 (= B-15) | **Reachable dependency vulnerability GO-2026-4550** — incorrect secp384r1 CombinedMult in `github.com/cloudflare/circl` (v1.6.2, pulled in via ProtonMail/go-crypto → openpgp used by the deltachat/xmpp crypto paths). | Bumped to **v1.6.3** (the fixed release) with `go get` + `go mod tidy`; go.sum pins updated. | govulncheck IS the failing test here: pre-bump it reported "Your code is affected by 1 vulnerability from 1 module" with reachable traces; post-bump (same command, same flags: inside `nix develop`, `GOFLAGS=-tags=goolm`) it reports **"No vulnerabilities found. Your code is affected by 0 vulnerabilities."** + full gate green (vet/tests/race against the new dep) |

## Fixed — slice 240 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-18 (= B-14) | **Reaction-"more" button rendered mojibake with an invisible control char.** The literal was `C3 A2 C2 8B C2 AF` — a Latin-1 double-encode of "⋯" (U+22EF) that embeds raw U+008B, so the message actions-row button showed a garbage/invisible glyph (staticcheck ST1018). | Fixed as `reactionMoreLabel = "⋯"` const; the package-wide source scan (decoding each .go and rejecting any rune in U+0080–U+009F) found exactly this ONE site repo-wide — and now guards against any future double-encode. | source-scan RED evidence: the slice-240 python scan reported `go/gui/menu.go 586 ['0x8b']` as the repo's only C1-control hit; `TestGUISourcesHaveNoControlCharLabels` compiles-RED pre-patch (const undefined) and becomes the behavioral regression guard after (scan must stay zero + const must equal "⋯") — green post-patch, octet 0x8B gone from the file |

## Fixed — slice 239 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-17 (= B-13) | **Dedicated signup form was never wired.** `authCard` had no `AuthStateSignUp` case (git -S: the call site never existed since slice 193), so signup fell into the default branch → the generic single-line `authInput`: no First/Last split, no photo circle, and the pre-193 bug (last names silently merged/lost, engine expecting `first\nlast`) stayed LIVE while parity row 44 claimed it fixed. | Wired: `case engine.AuthStateSignUp: return a.layoutSignupCard(...)` in the dispatch. Parity row 44 restored to PRESENT on the strength of the test (miscount history kept visible in the row). | RED: `TestSignupStepRendersDedicatedCard` drives the REAL `authCard` through the input router and clicks the Create-account band — failed pre-patch with `toast=""` and the B-13 diagnosis (the assertion is the card's OWN validation wording, which the generic input can never say); green post-patch (toast = "Enter your first name"), full gui suite + `-race` clean |

## Fixed — slice 238 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-16 (= B-12) | **Paid-media star wall was never wired.** `layoutPaidMediaWall`, `paidUnlockClickable` and `confirmUnlockPaidMedia` had zero callers since slice 181 (`git log -S` = one commit), so locked paid posts showed no wall, no price, no Unlock button — parity row 153's whole chain was unreachable, and the confirm card's only state-writer sat inside the dead function. Wired through the bubble's media row via a new pure dispatch (`paidBubbleWall`: locked-only, §1.10), AND fixed a second | defect found while wiring: `widgets.init()` never created `paidUnlockBtns`, so the FIRST wall render would have written a nil map and panicked. Parity row 153 restored to PRESENT on the strength of the new test (history of the miscount kept visible in the row). | RED: `TestPaidWallRendersInBubbleAndArmsConfirm` drives the REAL `messageRow` layout through the real input router and clicks the wall's Unlock button — failed pre-patch with "paid wall never armed the unlock confirm — the star wall is not wired into the bubble (B-12)"; green post-patch (confirm armed with stars=25 + right msgID), guard subtests prove unlocked/plain messages never arm it; full gui suite + `-race` clean |

## Fixed — slice 237 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-15 (= B-11) | **XMPP roster never loaded.** The `<ver>` field tag (`xml:"jabber:iq:roster query>ver,attr"`) is structurally invalid — encoding/xml rejects `,attr` on a chained path — so `xml.Unmarshal` errored on EVERY roster payload; `handleRosterPush` returned silently on that error and `requestRoster` ignored it outright → `c.roster` stayed empty forever: contacts never appeared, and roster-push removes were dropped with them. | Fixed by extracting `xmppParseRoster` (correct XEP-0237 element tag) and using it at both sites, which now LOG failures instead of swallowing them — an invisible empty roster must never again masquerade as "no contacts". | RED: `TestXMPPParseRoster` failed to build pre-patch (seam undefined; the audit's standalone probe had already shown `err=xml: query>ver chain not valid with attr flag, items=0` on every input); green post-patch across items/ver/groups, the `subscription=remove` payload, and the malformed-XML error path; full cores suite + `-race` clean; staticcheck re-run shows SA5008 gone |

## Fixed — slice 236 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-14 (= B-10) | **Multi-chunk streams completed after chunk 0.** `finishFetchLocked`'s done-check looped `for _, ok := range s.have` but broke on the first `ok` — inspecting only slot 0 (staticcheck SA4004, loop unconditionally terminated; present since slice 228, `git log -S`). With `streamChunk = 512 KiB`, EVERY file bigger than one chunk (i.e. every video) set `done` when the first chunk landed → `onDone` ran the DB promotion `download_state=DownloadComplete` + emitted `EventDownloadComplete` while most of the sparse file was still unfetched holes → "complete" consumers could read zero-filled gaps (the §1.10 class), play-on-done fired on partial bytes, badges lied. | Fixed: completion = EVERY slot present, evaluated under the stream lock; exactly-once kept by the existing `justDone` / `onDone = nil` handoff. | RED: `TestMediaStreamDoneOnlyAfterLastChunk` failed pre-patch with "onDone fired after chunk 0 of 3 (1) — stream promoted complete while 2/3 of the file is still holes"; green post-patch, full engine suite + `-race` over all stream tests clean, staticcheck SA4004 gone |

## Fixed — slice 234 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-12 (= B-4) | **Stream readers outlived their view.** Entries kept `src` open from first play until reset/replace/evict (≤48 entries) — leaving a chat or closing the viewer held fds (Android ≈1024 cap) for the rest of the session. Both view boundaries now release: `closeViewer` resets the viewed clip, and an actual chat change runs `leaveChatMediaReset` → `resetAll` (drop every entry, close every reader, return the ids so their audio can pause). Lock discipline kept: `resetAll` takes the cache lock itself; the openChat hook runs WITHOUT `a.mu` (`closeSrc` reaches the engine's file guard). | fds freed at the view boundary instead of at eviction; the next visit re-parses honestly (no stale verdicts) | `TestCloseViewerReleasesStreamSource` (behavioral RED pre-patch: "stream reader still open after its view closed"), `TestH264CacheResetAllReleasesEverything`, `TestLeaveChatReleasesStreamSources` + full gui suite + `-race` |
| F-13 (= B-9, found while fixing B-4) | **Orphan sound across a chat switch.** The loop's audio re-arms only from the draw path (its own comment: "a stopped video never restarts its sound") — but NOTHING paused the CURRENT track on chat change (zero `videoAudioPause` call sites in `openChat`/`closeViewer` pre-patch), so a note's audio kept playing out behind the next chat (§1.10, bounded by track length but real). Logged as its own entry rather than folded into F-12; both view-boundary seams now pause their own track — own-track-only via `videoAudioMine`, so leaving a chat can never pause someone else's music. | sound without a picture when the user has moved on | the two hooks run inside `TestCloseViewerReleasesStreamSource` / `TestLeaveChatReleasesStreamSources` (glue over the already-tested videoaudio logic; the nil-engine guard makes the call a no-op in unit tests — the audio half is review-evidenced, not overclaimed) |

## Fixed — slice 233 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-11 (= B-3) | **Duplicate chunk fetches.** `ensureChunk`'s fetch ran outside the bookkeeping lock with no coalescing: every reader that missed an already-started fetch issued its OWN RPC. Now per-chunk single-flight: one claim per chunk (`fetching[c]` channel), waiters block and re-check the bitmap; a failed leader hands the chunk over so the next waiter retries SERIALLY (no error herd); success wakes only AFTER `have[c]` is set; the lock is still never held across the RPC (Close/completion must not wait on the network), and results are coalesced, never cached (an entry exists only while a fetch runs). | measured pre-patch — the row's evidence line quantified with a delayed fake source: 8 readers of one chunk = **8 RPCs**; 4 concurrent whole-file readers = **126 RPCs vs 32** and **1,032,192 B fetched for a 262,144 B file (3.9× the bytes, ~4× the network cost of the same playback)** | `TestMediaStreamSingleFlightPerChunk` (both subtests RED pre-patch: `RPCs = 8, want 1` and `calls = 126, want 32`) + every prior stream test still green (external contract unchanged) + `-race` on the whole stream suite |

Readahead (the other half of the old "next step") is an optimization,
not a bug — not implemented, not opened as an entry; the duplicate-RPC
gap this row described is closed.

## Fixed — slice 232 (2026-09-24)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-10 (= B-2) | **Stale FILE_MIGRATE limitation — PREMISE CORRECTED.** There is no migration gap: gotd's invoker chain intercepts `FILE_MIGRATE`/`STATS_MIGRATE` and reruns the call on a sub-connection to the target DC (`telegram/client.go:280` `c.invoker = invokeDirect` → `invoke.go:70` → `sub_conns.go`), and every client we hold rides that chain (`t.api = tg.NewClient(rpcGuard{next: t.client})`); `DownloadFile` (downloader → same api) and the thumb path never had migration code either — they work BECAUSE gotd migrates. The old comment claimed errors "surface to the caller"; what actually surfaces is a REDIRECT failure, now wrapped with that context while the raw error stays `errors.Is`-visible for the engine's retryable-read classification. | a wrong limitation comment invites the next session to re-implement a gap that does not exist (the row's own "next step" said "mirror DownloadFile", which does not migrate either — the premise was wrong twice) | `TestReadFilePartWrapsRedirectFailureWithoutSwallowing` (RED pre-patch: raw `rpc error code 400: FILE_MIGRATE` with no context) + `TestReadFilePartReturnsBytesAndForwardsRange` (success-path guard: bytes + offset/limit/precise/location fidelity) |

Evidence chain walked line by line before deciding: `withAPI` →
`guardedClient() = tg.NewClient(rpcGuard{next: t.client})` →
`telegram.Client.Invoke` → `chainMiddlewares(invokeDirect)` →
FILE_MIGRATE branch → `invokeSub(dc)` → cached sub-connection.
gotd has no test of its own for that branch (grep), so the chain was
read directly from the pinned module (v0.161.0).

## Fixed — slice 231 (2026-09-23)

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-8 (= B-1) | **QuickLZ send-side.** PREMISE CORRECTION first: the RX decompressor (`tsQuickLZDecompress`, fragmented AND non-fragmented) and TX fragmentation already existed — only the compress side was a TODO, so every command > 487 B went out as raw fragments while reference clients compress first. `tsQuickLZCompress` is a faithful port of official quicklz.c 1.5.0 level 1 in the TeamSpeak build configuration (streaming off, QLZ_PTR_64 — including the "stored position 0 reads empty" table quirk and the ratio give-up); `tsSendCommand` compresses first, ships one `0x40` packet when the stream fits, fragments the compressed stream when it does not, and falls back to raw fragmentation when give-up makes the stored form bigger than the input. | measured, not assumed: today's public server ACCEPTS raw multi-packet C2S (pre-fix live run — 615 B command → semantic permission reply = decoded end-to-end), so B-1 was an efficiency/compat gap, not an outage; but the wire cost was real: a 2600 B command went out as 6 packets instead of 1 | `TestTS3QuickLZCompressByteIdenticalToOfficial` (19 gcc-built golden vectors, byte-identical), `TestTS3QuickLZRoundTripOfficialStreams`, `TestTS3QuickLZCompressRejectsEmpty`, `TestTS3SendCommandCompressesLargeCommand` (4 subtests: 580 B→1 pkt, 2600 B→1 pkt, 1500 B noise→raw 4-fragment no-0x40, small→unchanged), `TestTS3SendCommandPacketIDsSequential`, live `TestTeamSpeakLiveLargeMessage` (pre + post fix) |

| F-9 | **Date-dependent tests found by the gate.** `TestHeaderLastSeenExact` / `TestSameCalendarDay` assumed "now minus 90 min / 1 h" is always the same calendar day — false between 00:00 and 01:30 local. The suite went RED at 00:03 while production was CORRECT (it rendered yesterday's date for a yesterday timestamp). Tests now anchor to noon of today's date: deterministic at every wall-clock time; only test code changed. | a red gate hides real regressions; the bug was in the test's wall-clock assumption, not the renderer | the failing runs themselves (repro in the 00:00–01:30 window pre-patch) + both tests green at any hour post-patch |

Session notes for honesty: guest text on ts.arcticblaze.net is now
permission-denied for big messages in both modes (permission drift
since the 2026-09-10 run), so peer delivery is no longer measurable
there — the live oracle is "semantic reply = decoded end-to-end",
delivery opportunistic; `TestTeamSpeakLiveRoundTrip` still passes (its
so-called echo is actually a third-party bot message — label corrected
to what it proves, S2C delivery). Caught on the first run: `same7` was
given an already-shifted position while it shifts internally (double
subtraction → panic `index -2`), one-line fix. Also known: the edit+
read/test-same-file pairing rule was violated twice more this slice.

## Fixed — slice 230 (2026-09-23)

Audit method: read the slice-228/229 streaming paths line by line looking
for wrong behaviour; every row below has a test that fails without the
fix.

| # | Bug | Why it mattered | Test |
|---|-----|-----------------|------|
| F-1 | **Stream/download write race.** `RequestDownload`/`executeDownload` never checked for an open stream, and `MediaManager.Cancel` cannot touch an already-dequeued job — `os.Create` (O_TRUNC) would truncate the sparse file under the playing picture. Auto-prefetch (`maybeAutoDownload`, scroll-in prefetch) makes the collision reachable with no user gesture. Now a per-file guard makes the writers mutually exclusive in BOTH directions (`ErrStreamActive` download-side, `ErrStreamBusy` stream-side) and duplicate jobs for one key skip. | broken playback; two writers on one path | `TestRequestDownloadRefusedWhileStreamOpen`, `TestOpenMediaStreamRefusedWhileDownloadActive`, `TestExecuteDownloadSkipsWhileStreamOpen`, `TestStreamGuardSurvivesDoubleClose` |
| F-2 | **Re-download of a completed file.** `RequestDownload` on a complete row re-`os.Create`d the finished file (truncate under readers) and enqueued needless network work; markers waiting on that row never fired. Now completes instantly with `download_complete` (no queue, no core call). | dead taps + file torn under readers | `TestRequestDownloadCompletesInstantlyWhenFileOnDisk` |
| F-3 | **fd leaks on the streamed-player path.** `ensureH264StreamPlayer` leaked its reader on every decline (already-playing re-open, parse in flight, invalid args); on parse failure the close lived only in caller closures; cache entries dropped readers on reset/replace without closing. Now ensure owns the reader on EVERY path and the cache closes `src` on reset/replace/evict. | fd table grows for the whole session (Android limit ≈1024) | `TestEnsureStreamPlayerClosesDeclinedReader`, `TestEnsureStreamPlayerClosesReaderOnParseFailure`, `TestStreamSourceClosesOnResetReplaceEviction` |
| F-4 | **Dead eviction path.** The h264 cache's size cap lived only in `get()`, which nothing calls for this cache — entries and producer goroutines grew without bound. Caps now enforced at every insert via a shared `evictLocked` (which also closes sources). | unbounded memory + goroutines | eviction subtest in `TestStreamSourceClosesOnResetReplaceEviction` |
| F-5 | **Transient failure pinned permanent.** Any `ParseSeek` error called `failParse`: one dead chunk fetch during parse marked the clip unplayable forever. New `h264vid.ErrDecodeFailed` + `IsPermanent` separate "verdict about the bytes" (pin) from "failed to read them" (retry). | §1.10 — a good clip dead-ended by one network blip | `TestParseSeekClassifiesDecodeStageFailure`, `TestParseSeekGarbageNotPermanent`, `TestIsPermanentClassifiesVerdicts` |
| F-6 | **Orphan sound / wrong join offset.** The completion-time audio join could start behind a viewer clip that had already played through (sound with a frozen last frame) and ignored loop phase. `streamJoinAt` wraps the offset with the picture and refuses ended clips. | fake playback (§1.10) + desync | `TestStreamJoinAt`, rewritten `TestStreamShouldJoinAudio` |
| F-7 | **Frozen frame with running sound.** A producer dying mid-play (fetch error) left the cache `playing` — static frame while the engine kept playing audio, taps did nothing. The draw path now heals: reset the entry + pause the audio; the next tap re-opens the source. | §1.10 stuck state | source-close tests + review (the draw call itself is gtx-bound, not unit-tested) |

Session notes kept for honesty: the test oracle itself was broken once
(`countCloser` was missing its `Close()` method, so every close
assertion silently passed on a no-op — the checker failed the checker),
and the "audio-only" fixture assumption was wrong (the aacaud fixtures
are full video files, measured with ffmpeg) — fixed by generating and
sha-pinning a real one: `h264vid/testdata/audio_only.mp4` =
`ade3487e…b6bb7`. Also investigated and dismissed: green run
`dd00b612` is the gh-pages workflow's own commit, not on `main`.
