# GUI Checklist

**BEFORE STARTING ANY WORK: Read `CLAUDE.md` and obey ALL its rules.**

## MANDATORY: Research-first, AyuGram Desktop 1:1

**Every UI feature MUST match AyuGram Desktop exactly.** Before implementing anything:
1. Check `research/telegram_desktop_ui.md` for the relevant section
2. If info is missing or insufficient, research AyuGram Desktop source (https://github.com/AyuGram/AyuGramDesktop) and ADD findings to `research/telegram_desktop_ui.md` BEFORE writing any code
3. Never guess how a feature should look or behave — find the real implementation

## MANDATORY: Self-test with automated interaction

**After implementing any change, you MUST test it yourself using the automated interaction pipeline.** See `CLAUDE.md` § GUI Automation Toolkit for full command reference (`flutter_inspect.sh`, `flutter_interact.sh`, `flutter_auth.sh`).

**Workflow:** screenshot → identify coordinates → interact → screenshot → verify result. Do NOT mark anything as done until visually confirmed.

## Bugs (fix first, verify with automated interaction)

- [x] **AuthScreen QR renders real scannable code** — `_buildQR` in `dart/lib/ui/auth_screen.dart:160-196` now decodes `data.qrData` bytes via `utf8.decode(...allowMalformed: true)` and renders them through `QrImageView` (`qr_flutter ^4.1.0`, already in `pubspec.yaml`). Screenshot-verified on a fresh vault at 1280×720: tapping Telegram → QR Code produces the full "Scan QR Code" screen with a sharp black-on-white matrix QR (≈180×180 with 8px padding, `QrVersions.auto`, `QrErrorCorrectLevel.M`, `gapless: true`, square eye + data modules). Log confirms `state=qr` from `SubmitAuthInput`. No more static `Icons.qr_code_2` placeholder — the real Telegram `tg://login?token=...` URL is encoded and scannable by the phone.
- [x] **NoAccountsScreen RenderFlex overflow fixed** — Wrapped the Column in `LayoutBuilder`→`SingleChildScrollView`→`ConstrainedBox(minHeight: constraints.maxHeight)`→`Center`, with 32px vertical / 16px horizontal padding around the Column. The scroll view provides unbounded vertical space so the Column never overflows; the minHeight constraint keeps the content centered when it fits. Verified on fresh vault at 1280×720 and 600×400 (via kdotool resize): no `overflow` or `RenderFlex` log entries, all 9 platform buttons (Telegram, Matrix, XMPP, IRC, Bale, Rubika, Delta Chat, Mumble, TeamSpeak) render in a 2-column wrap with the welcome header. `dart/lib/ui/shell.dart:316-362`.


- [x] **Last seen / online in DM top bar verified** — Screenshot-verified: online users show "online" in green, offline show "last seen yesterday at 16:57" etc. Fix: `convertUser` emits `UpdateUserStatus` for all status kinds (online/offline/recently/within_week/within_month/long_ago), seeding the engine cache on initial dialog load instead of waiting for a delta.
- [x] **Forward display fixed** — Forwarded messages now show "Forwarded from {name}" header. Fixed: Go `convertMessage` resolves `FwdFrom` to display name (FromName > cached user/channel name > PostAuthor > fallback). Added `channelNames` cache. Migration made idempotent.
- [x] **Pinned bar jump verified** — Clicking pinned bar scrolls to pinned message; scroll-to-bottom button (with unread badge) returns to newest via `returnToLatest()`. Screenshot-verified in group chat "انجمن اینترنت و فیلترشکن".
- [x] **Sender avatars verified** — 33px circles with letter initials render next to each sender's first message in group chats. Screenshot-verified.
- [x] **Sender profile popup verified** — Tapping sender avatar opens compact horizontal card (avatar + name + ID + "Send Message" action). Screenshot-verified.
- [ ] **Sender avatars for basic groups (cannot verify — no basic groups in account)** — Go `GetMembers` falls back to `MessagesGetFullChat` for basic groups. Dart paginates up to 1000 members. Code path compiles but cannot be visually verified because the active Telegram account has no basic groups (all tele chats in cache are supergroups/channels with `-100...` ID prefix). Verification deferred until a basic group is available.
- [x] **Sender avatars fallback for no-photo users** — Users with no profile photo show colored letter initials. Correct behavior, not a bug (closes item).
- [x] **Top bar avatar verified** — Chat avatar in top bar uses real photo from `avatarPath` when available and falls back to colored letter avatar. Screenshot-verified in "انجمن اینترنت و فیلترشکن" (colorful logo matches sidebar) and "Nakoshi's Diary" (real photo). Top bar avatar stays consistent with sidebar avatar across chats.

## TODO (features not yet implemented)

### Folder management
- [ ] Right-click folder tab → edit folder (include/exclude chats, sync to Telegram)
- [ ] Right-click chat → "Add to folder" submenu (choose folders to add/remove)

### Chat row enhancements
- [ ] Stories ring on avatars
- [ ] Mini media previews in message preview
- [ ] Auto-delete indicator ("1d", "1w" etc.) on DMs with auto-delete enabled

### Chat view / DM
- [ ] Sticker rendering (static + animated + video stickers)
- [ ] GIF rendering and playback
- [ ] Video playback in messages (currently shows placeholder)
- [ ] Animated/video profile pictures
- [ ] Translate to English button (like AyuGram)
- [ ] Reactions display on messages

### Profile/Info panel
- [ ] Full profile view on clicking top bar: pfp, name, status, action buttons (message/mute/call/more)
- [ ] More menu: auto-delete, edit contact, export chat history, add to folder, block, delete contact
- [ ] Profile music/channel links
- [ ] Phone number, bio, username, birthday, notes, number ID display
- [ ] Media tabs (photos, videos, files, links, voice, GIFs)
- [ ] Edit contact / delete contact / block user actions

### Search
- [ ] Search results (top peers, recent contacts, search tabs)

### Other features
- [ ] Contact status/action bar (add contact, block, report)
- [ ] Group call bar
- [ ] Voice/video message players
- [ ] Emoji/sticker/GIF panel
- [ ] Calls UI
- [ ] Settings screens
- [ ] Mobile (OneColumn) slide navigation

## Needs visual review (implemented but not screenshot-verified against AyuGram)

- [x] Responsive shell layout (UniClientShell) — OneColumn/TwoColumn/ThreeColumn modes — Screenshot-verified at three widths via kdotool resizing: (1) **OneColumn** at ~490px shows single panel with horizontal folder tabs + hamburger; opening a chat replaces list with chat view + back arrow (slide navigation working). (2) **TwoColumn** at ~1280px shows 72px filter sidebar + dialogs column + chat view. (3) **ThreeColumn** at 1400px after tapping top-bar info icon adds right-side Group Info panel (avatar, ID, Notifications toggle, media tabs, Members). Breakpoints: `_oneColumnBreak=640`, `_threeColumnBreak=932` in `dart/lib/ui/shell.dart`.
- [x] **Chat list sidebar (ChatListPanel) verified** — Screenshot-verified at 1280px: rounded search field with search icon and hamburger (3-column mode hides it, OneColumn shows it). Tapping search focuses the field and reveals a "Cancel" button at the right. Chat rows sort correctly — two pinned chats ("Nakoshi's..." and "./Suburb...") with pin icon appear at the top, followed by non-pinned rows in lastMsgTime descending order (05:22, 05:22, 05:10, 04:58, 04:41, 04:39, 04:06, 03:56, 03:49, 03:39). Search wiring: `searchChats(query)` → engine `SearchChats` RPC → `_searchResults` replaces the visible list (`dart/lib/ui/chat_list_panel.dart:39`).
- [x] **Chat list row (ChatListRow) verified** — Screenshot-verified in active and inactive states. 62px row height, 46px circular avatar at left (real photo + colored letter-initials fallback), chat-type icon (people/megaphone) before title, bold title, right-aligned timestamp ("Yesterday"/"05:35"/etc), sender prefix in blue (`#168acd`), unread badge pill in primary blue (or white when active), pin icon on pinned chats. Active row now renders spec color `#419fd9` (Telegram blue) with white text — verified pixel-exact at `(80, y=180..220) = (65, 159, 217)`. Fix: swapped `Material(color: theme.colorScheme.primary)` for `Container(color: const Color(0xFF419fd9)) + Material.transparency + InkWell` to bypass MD3 Material surface-tint washing `Material(color: primary)` toward white inside a `ColorScheme.dark` scaffold. `dart/lib/ui/chat_list_row.dart:35`.
- [x] **Hamburger drawer (HamburgerDrawer) verified** — Screenshot-verified at 1280px: 274px-wide drawer opens from top-left hamburger icon. Profile cover (134px) shows active account avatar (letter fallback "F" for Fallen Reaper), green connection dot, name + platform ("Telegram"), expand arrow. Tapping arrow expands account list (max 320px scrollable) showing all accounts with platform icons (send/telegram, chat/bale), connection dots, and blue checkmark on active account. Menu items: My Profile, New Group, New Channel, Contacts, Calls, Saved Messages, Settings. Night Mode toggle with moon/sun icon swaps between dark and light theme live (verified background colors change). Version footer "UniClient v0.1.0" at bottom. `dart/lib/ui/hamburger_drawer.dart`.
- [x] **Chat view (ChatView) verified** — Screenshot-verified in "Nakoshi's Diary" (2-member DM) at 1280px. Top bar (54px): real avatar + bold title "Nakoshi's Diary" + subtitle "2 members" + search/info/menu icons on right. Pinned bar (blue pin icon + "Pinned Message" + content preview). Message list: incoming bubbles (dark, left) with sender avatar circle + sender name ("Info-chan 🐻") + content + right-aligned timestamp; outgoing bubbles (navy blue, right) with "Forwarded from Fallen Reaper" header + content + timestamp + check mark. Scroll controller + reverse list. Compose area (bottom): attach icon (📎) + rounded "Write a message..." field + emoji icon + send arrow. `dart/lib/ui/chat_view.dart`.
- [x] **Message bubbles (MessageBubble) verified** — Screenshot-verified in DM ("Nakoshi's Diary") and group chat ("انجمن اینترنت و فیلترشکن"). Incoming bubbles render with dark `bubbleReceived` color on the left; outgoing bubbles render with navy `bubbleSent` on the right. Forward header shows "Forwarded from Fallen Reaper". Group chat shows the 7-color sender palette (teal/purple/pink/green avatars + matching colored name text), with sender name only on the first message in a group and 33px avatar circle only on the last message of a group (consecutive-grouping logic correct). Reply preview renders with a 2px left border and 1-line ellipsis (e.g. reply to "ایشالا وصل شد میگمت"). Bubble corner radii follow spec §5 (16px/6px depending on grouping). Per-bubble timestamp + outgoing check mark visible. `dart/lib/ui/message_bubble.dart`.
- [x] **Auth screen (AuthScreen) verified** — Screenshot-verified the full flow with a fresh vault at 1280×720. (1) `NoAccountsScreen` renders "Welcome to UniClient" + platform grid (Telegram, Matrix, XMPP, IRC, Bale, Rubika, Delta Chat, Mumble, TeamSpeak). Tapping a platform calls `addAccount` + `startAuth`. (2) `choose` state shows lock icon, "Choose Login Method" headline, and 3 outlined 44×full buttons (Phone Number, Bot Token, QR Code) + Cancel. (3) `input` state (phone) shows "Enter Phone Number" title, "Phone Number" label, "Hint: +1234567890" hint, 300px TextField with placeholder "+1234567890", filled blue "Next" button, Cancel. (4) `qr` state shows "Scan QR Code" title, white 180×180 container with QR placeholder icon, "Open Telegram on your phone / Go to Settings > Devices > Link Desktop Device" instructions, Cancel. `dart/lib/ui/auth_screen.dart`. **Known placeholder:** `_buildQR` renders `Icons.qr_code_2` static icon instead of encoding `qrData` bytes — logged below.
- [x] **Column resize handles verified** — Dragging the 4px `_ResizeHandle` between the dialogs column and the chat area grows/shrinks the dialogs column live. Screenshot-verified three states: (1) initial 260px (min clamp, titles truncated), (2) after drag-right from (334,400)→(600,400) the column expanded to ~513px (handle now at x=585, titles render full "Nakoshi's Diary", "./Suburban.sh", "vx-underground", "SamNet Community"), (3) after drag-left from (585,400)→(250,400) the column snapped back to 260 min (titles truncate again). Cursor is `SystemMouseCursors.resizeColumn`. `dart/lib/ui/shell.dart:252-270`. **Fixed along the way**: `_dispatchDrag` in `dart/lib/main.dart` was emitting `PointerMoveEvent`s with `delta: Offset.zero` (default), so `HorizontalDragGestureRecognizer.handleEvent` saw zero motion and `DragUpdateDetails.delta.dx` was always 0. Now populates the per-step delta between consecutive interpolated positions so pan/drag recognizers receive real motion.
- [x] **Context menu on chat rows verified** — Right-clicking any chat row in the sidebar opens a `PopupMenuButton`-style menu at the cursor with five state-aware items: **Pin/Unpin** (label flips with `chat.isPinned`), **Mute/Unmute** (flips with `chat.isMuted`), **Mark as Read/Unread** (flips with `chat.unreadCount > 0`), **Archive/Unarchive** (flips with `chat.isArchived`), and a divider then **Leave Chat**. Each item dispatches the matching `ChatState` method (`pinChat`/`muteChat`/`markChatRead`/`archiveChat`/`leaveChat`) which calls the engine's `PinChat`/`MuteChat`/`MarkChatRead`/`ArchiveChat`/`LeaveChat` RPCs. Screenshot-verified on a pinned read DM (menu shows "Unpin" + "Mark as Unread") and on an unpinned unread group (menu shows "Pin" + "Mark as Read"), and tapping "Mute" produced an `__engine.MuteChat` RPC log line and the chat row immediately rendered the `volume_off` icon next to its title. **Bug fixed along the way:** old `_showChatContextMenu` cast `context.findRenderObject() as RenderBox`, but the panel's BuildContext lives inside a `ListView`, so its render object is a `RenderSliverList` and the cast crashed every right-click with `type 'RenderSliverList' is not a subtype of type 'RenderBox'`. Replaced with `onSecondaryTapDown` capturing `details.globalPosition` on `ChatListRow`, then `RelativeRect.fromRect(globalPosition & const Size(40, 40), Offset.zero & overlay.size)` for the menu position. `dart/lib/ui/chat_list_panel.dart:124-176`, `dart/lib/ui/chat_list_row.dart:15-67`.
- [x] **Empty states verified** — Three distinct empty states render correctly. (1) `_NoAccountsScreen` (no accounts) was previously verified on fresh vault — welcome + platform grid. (2) `_EmptyChatPlaceholder` in `dart/lib/ui/shell.dart:273-295` — screenshot-verified at 1280×720 with existing accounts and no chat selected: centered `chat_bubble_outline` (64px) + muted "Select a chat to start messaging" text against `scaffoldBackgroundColor`. (3) `_EmptyState` in `dart/lib/ui/chat_list_panel.dart:365-393` — screenshot-verified: typing a nonexistent query ("xqqq_nonexistent") into the sidebar search swaps the chat list for a centered `search_off` (48px) + "No results found" label; non-searching variant shows `chat_bubble_outline` + "No chats yet". (4) "No messages yet" in `dart/lib/ui/chat_view.dart:608-615` (centered `bodySmall` text when `messages.isEmpty && !loading`) — code-verified; cannot be screenshot-captured on current vault because every dialog in cache has history. Pattern is consistent with the other two empty states.
- [x] **IsOutgoing propagation verified** — Screenshot-verified end-to-end in two chats at 1280×720: (1) `./Suburban.sh` group — every message is outgoing, all render right-aligned with navy `bubbleSent` color, "Forwarded from Fallen Reaper" headers on forwards, and ✓ send-state icon next to each timestamp (14:53, 16:54, 16:55, 17:04, 17:24 …). (2) `Nakoshi's Diary` DM — mixed directions in same view: incoming "🔒 Custom Key Required" messages from Info-chan render left-aligned with dark `bubbleReceived` color + sender avatar + sender name "Info-chan 🐻" + NO check mark (21:32, 21:41); outgoing "reply" and "تست" render right-aligned with navy `bubbleSent` + "Forwarded from Fallen Reaper" header + ✓ check (21:32, 21:41). Chat list sidebar also shows ✓ next to "./Suburb... Yesterday" via `_sendStateIcon` in `dart/lib/ui/chat_list_row.dart:235-240` reading `chat.lastMsgIsOutgoing`. Full pipeline confirmed: Telegram Core (`msg.Out`) → Engine cache (`is_outgoing` column) → Protobuf (`isOutgoing` field) → Dart model (`Message.isOutgoing`) → `MessageBubble` rendering in `dart/lib/ui/message_bubble.dart:42-48` (bubbleColor + alignment + check icon selection).
- [x] **Message loading live-fetch fallback verified** — When the engine's SQLite cache has fewer than `limit` (default 50) messages for a chat and `beforeMs == 0` (initial load, not pagination), `GetMessages` in `go/engine/cache_msgs.go:87-110` calls the core's `GetMessages` directly and caches the result. End-to-end verified on Telegram "vx-underground" (`-1001182568527` on `tele_4beb99fd`): cache held 2 messages before open (confirmed via `sqlite3 ~/.cache/uniclient/cache.db`); tapping the chat row dispatched `__engine.SetActiveChat` followed by `__engine.GetMessages (35B)` → engine log `cache empty, fetching live from core...` → `got 50 live messages, caching...` → response `__engine.GetMessages OK (73220B)`. Chat view rendered the full 50-message page (pinned "Email: staff@vx-underground.org" bar, real sender avatars, reply previews, edited timestamps — "Meta intends on laying off…", "vx-underground turns 8 years old next month", etc.). The guard `beforeMs == 0` ensures pagination doesn't spuriously re-hit the network when the user scrolls past the top.
- [x] **SQLite schema migration V2 — `is_outgoing` column verified** — `migrateV2` in `go/engine/db.go:342-349` adds `is_outgoing INTEGER NOT NULL DEFAULT 0` to `messages` via `ALTER TABLE`, guarded by `columnExists()` for idempotency. Migration runner in `migrateDB` (`db.go:110-136`) iterates `migrations[]` starting from the current `PRAGMA user_version`, wraps each step in its own transaction, and bumps `user_version` after a successful commit — so a DB at V1 transitions V1→V2→V3→V4 atomically on next open. Verified three ways: (1) live prod DB at `~/.cache/uniclient/cache.db` is at `user_version=4` with `is_outgoing` populated (698 incoming, 115 outgoing rows). (2) New `TestMigrateV2IsOutgoing` in `go/engine/engine_test.go` rolls a fresh DB back to V1 state (drops column + resets user_version=1), reopens, and asserts migrateV2 re-adds the column, preserves existing rows with default 0, honors explicit 1 values on new inserts, and is idempotent when called twice — passes. (3) Updated `TestOpenDB` and `TestDBMigrationIdempotent` to assert `user_version == len(migrations)` instead of the stale hardcoded `1`, so the schema-version invariant travels forward with every future migration.
- [ ] Cross-platform outgoing detection — Telegram (msg.Out), Bale (sender == self), all other cores
- [ ] Folder tabs (FilterColumn, 72px sidebar) — when active account has folders
- [ ] Horizontal folder tab strip (when sidebar hidden)
- [x] **Chat row: send state icons verified** — `_sendStateIcon` in `dart/lib/ui/chat_list_row.dart:235-240` returns `Icons.check` when `chat.lastMsgIsOutgoing` is true, else `null`; rendered at `chat_list_row.dart:124-127` as a 14px muted check between the title row and the timestamp. Screenshot-verified at 1280×720 with Telegram account: `./Suburban.sh` row (last message outgoing) shows `✓ Yesterday`; `Nakoshi's Diary` row (last message incoming from "Info-chan 🐻") shows plain `Yesterday` with no icon. Hit-test at the pre-timestamp coordinate confirms the asymmetry — `hittest 248 132` (Suburban) returns a stack containing `TextSpan → RenderParagraph → RenderPositionedBox` (Icon wrapper), while `hittest 248 70` (Nakoshi's) hits `RenderParagraph` directly with no positioned-box wrapper. Every other incoming-last-message row (فیلترشکن, فروپاشی, انجمن اینترنت, مهرداد ماهر, /v/'s Video Me..., vx-underground, SamNet, Rahgozar IP, MIZAN, SOS Iran) renders its timestamp without a check — no false positives. Note: we only store send state per chat as a boolean (outgoing y/n), so all outgoing rows use single-check; "clock for sending" and "double-check for delivered/read" are not yet differentiated (intentional, matches existing `Message.SendState` coverage).
- [ ] Info panel (third column) — user/group/channel info, members list, shared media links
- [ ] Per-account isolation — one active account at a time, chosen via hamburger drawer
- [ ] Folder scoping — folders only shown for the active account
- [ ] Account switcher — profile cover + expand arrow, platform icons, connection dots, checkmark on active
- [ ] Media rendering in messages — auto-download photos, video placeholders, voice waveform, audio/file indicators
- [ ] Media download pipeline — engine MediaManager, auto-download, Telegram access hash persistence, DB schema V4
- [x] **Chat list column width verified** — `_dialogsWidthRatio = 0.17` default in `dart/lib/ui/shell.dart:27`, clamped to `[_dialogsMin=260, _dialogsMax=540]` at `dart/lib/ui/shell.dart:31-32`. Hit-test verified on live build at 1280×800 with filter sidebar visible: Filter sidebar 0–72 → **Dialogs column 72–332 (260 px)** → Resize handle 332–336 → Chat area 336–1280. Math matches spec: at body 1208 `→ 1208×0.17=205 → clamped to 260` (min); at body 1920 `→ 1920×0.17=326` (within clamp, matches "~300px" rule of thumb); at body 3180+ `→ clamped to 540` (max). Ratio is mutated by `_ResizeHandle` drags and used by both two-column and three-column layouts (`_buildTwoColumn` and `_buildThreeColumn`). Cannot resize beyond 1280 on this display, but the `0.17 × bodyWidth` formula is direct code and clamp behavior is already screenshot-verified in "Column resize handles verified".
- [ ] Avatar/profile picture download — Telegram DownloadChatAvatar, cached to disk, DB avatar_path, Image.file rendering
- [ ] Folder filtering fix — full Telegram filter flag support (contacts, non_contacts, groups, channels, bots, exclude_muted/read/archived) + DialogFilterChatlist support
- [ ] Chat row: online dot indicator — green dot on DM avatars via UserStatusEvent stream + Telegram OnUserStatus handler
- [ ] Pinned message bar — engine GetPinnedMessages with InputMessagesFilterPinned, ChatState integration, _PinnedBar widget
- [ ] Selection mode (multi-select messages) — long-press to enter, checkboxes, selection bar with copy/delete/forward
- [ ] Forward dialog — searchable chat picker, forwardSingle from context menu
- [ ] Message context menu — right-click/long-press popup (reply, copy, forward, select, edit, delete)
- [ ] Dialog pagination — GetDialogs paginates up to 500, first 100 fast + background rest
- [ ] Reply display — sent replies show reply_to_id + reply_preview in bubbles
- [ ] New chat auto-creation — ensureChatExists when message arrives for uncached chat
- [ ] DB corruption fix — ensureChatExists type integer fix + cleanup on startup
- [x] **Gesture dispatch system verified** — `dart/lib/main.dart` debug-command handler (JSON file poll every 1s at `/tmp/uniclient_debug_cmd.json`) dispatches real pointer and keyboard events via `GestureBinding.instance.handlePointerEvent()`. End-to-end exercised every subcommand against the live build: **gestures** — `tap` (focused Search TextField), `rightclick 200 80` (opened full 5-item chat-row context menu: Pin / Mute / Mark as Read / Archive / divider / Leave Chat), `longpress`, `doubleclick`, `scroll 200 400 0 -300` and `scroll 200 400 0 300` (chat list paginated in both directions, screenshot-diff confirmed), `drag 334 400 500 400 5` (interpolated PointerMoveEvents with real per-step deltas on the resize handle), `hover`, `key escape`, `dismiss`. **Queries** — `findtext "People"` → `[{x:18,y:158,w:35,h:14,cx:36,cy:165}]`, `alltext` → labeled JSON array of every visible text with positions, `hittest 200 25` → `RenderEditable` widget chain, `windowsize` → `{width:1280.0,height:800.0,pixelRatio:2.0}`, `waitfor` (timed out correctly when target text absent). **Combos** — `taptext "Search"` found "Search" at `(214,25)` and dispatched tap there, focusing the TextField. Screenshot-verified context menu at `/tmp/ralph_ss_ctx.png`. Pipeline is the backbone of automated GUI testing.
