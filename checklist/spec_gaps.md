# telegram_desktop_ui.md — Outstanding Spec Gaps

Source: full end-to-end audit of `research/telegram_desktop_ui.md` (session 2026-04-18). Each entry cites a line number in the audited file. After every content fill, strike through the entry (`~~line N~~`) and note the new subsection added. Rework line numbers if major blocks get re-inserted.

Progress so far:
- ✅ §1 reorder: §41/§42/§43 now in file order; §45/§46 swapped to correct order.
- ✅ §37 heading: HTML entities normalized to `§37 — Desktop Notifications`.
- ✅ §50 Streamer Mode & Read Toggles (AyuGram) added.
- ✅ §56 Appendix A — Resolved Style Constants added (~90 tokens resolved, ~25 still need deeper grep).
- ✅ §57 Appendix B — Dark Theme Color Palette added (full day-blue / night tables).
- ✅ §32.15 Story Creation Editor rewritten (300 lines, 13 subsections, honest gaps flagged where desktop source lacks features).
- ✅ §34.17 Create Conference Call Box added (287 lines — documents the participant picker + invite-link flow; clarifies that title/schedule/record options don't exist at creation time in upstream).
- ✅ §1 gaps filled: Shadow Separators subsection + Adaptive Width Formula (three-column shrink algorithm) + Linux Titlebar (X11/Wayland/KDE/GNOME) subsection.
- ✅ §2 gaps filled: search-mode tabs dimensions, stories ring gradient (confirmed no premium variant), unread/mention/reaction badge metrics, dark-theme hover/active/ripple palette, archived row (collapsed row, 37px, always-muted badge), swipe quick-action spec.
- ✅ §3 gaps filled: profile cover dimensions, account-switcher rows (26px avatar + active-ring), menu row metrics (`mainMenuButton` padding + font + icons), night-mode toggle widget, footer (two-line FlatLabel + tooltip).
- ✅ §4 gaps filled: topBarBg resolved (aliases windowBg in both themes, no distinct tint), 42px avatar in 52×54 hit-area, right-side buttons chrome (topBarSearch 40×54 + 40px ripple), 44px menu toggle, 49px pinned bar (32px thumb + 2×36 accent stripe + historyPinnedBg hex), contact status action bar (flat FlatButtons with historyContactStatusMinSkip=16), selection mode (easeOutCirc 200ms, uppercase button labels, RoundButton numbers animation, setCornerRadii pill).
- ✅ §5 gaps filled: JumpDownButton metrics (52×62 hit + 42px disc, 480px show threshold, 150ms slide), date badge (msgServicePadding + msgServiceBg alpha per theme), bubble colors resolved (all 8 tokens × day/night — night shadows disabled), bottom info row (font, icon sizes, inverted-overlay rules), peer name palette (8 slots × day/night hex + confirmed 64-extended fetched from server via help.peerColors), selection checkbox (20px, 160ms with bgDuration 0.75, boxTextFgGood), service-message pill (radius = height/2, text style).
- ✅ §6 gaps filled: spoiler particles (3000/128px, 1.5-2px, 60 frames @ 33ms, 200ms reveal), album layouter formula (complete dispatch rules + ComplexLayouter scoring), stickers (no shadow, 1.49× premium, mirror direction based on !rightAligned), voice waveform (100 fixed samples, full colors, no cursor line, 30% hover overlay), round video arc (3px stroke inset, 12 o'clock start, clockwise via negative sweep), polls (18px radio, 400ms quiz shake at ±3° linear, fireworks particle counts + colors), live location ring (28px, 2px stroke, counter-clockwise, no pulsing dot), web preview (article/standard NOT pixel-driven — webpage type only, no YouTube-specific overlay).
- ✅ §7 gaps filled: compose strip dimensions + palette (§7.1), attach-menu reality check — desktop paperclip opens native OS picker not styled popup (§7.2), send button state machine + cross-fade morph (kWideScale=5, universalDuration=120ms) vs Voice↔Round Lottie swap (§7.3), voice-record bar (blob radii, lock widget, tap-lock vs hold, video-record mode, TTL ring) (§7.4), bot keyboards — reply sticky vs inline (§7.5), drag-drop overlay (five states, rounded card not full-screen wash) (§7.6), SendFilesBox — the real mode-picker (caption/grouping/compression toggles, per-item context menu, AyuGram "Send as sticker") (§7.7).
- ✅ §8 gaps filled: cover compression math — three-snap scroll model (0/112/180px, easeOutQuint 260ms) driven by FlexibleScrollHelper not continuous interpolation (§8.1), action-button row — max 3 primary + overflow "More", per-button paint scaling, mute Lottie crossfade (§8.2), **shared-media has NO horizontal tab strip** — uses vertical type-button stack in Saved-Messages view + full-panel sub-sections via showSection elsewhere (§8.3), members list (42px avatar, pill admin tag, no online-dot, search inline currently commented-out) (§8.4), grid columns computed formula (infoMediaMinGridSize=82px, columns=floor((w−4)/84)) (§8.5), additional findings — cover gradient, story ring, pinned gifts, stars rating, bot About, business hours, layer-mode Wrap enum (§8.6).
- ✅ §9 gaps filled: popup menu chrome (radius 8px, 200ms open sineInOut, item padding 54/8/17/8 with icon = ~29px, shadow blur 5px) + full day/night palette (§9.1), attention-style items — **red icon, normal-colored text** by default (`menuWithIconsAttention` only for Leave-group confirmations) (§9.2), reactions strip geometry (40px strip, 32px slot, 26px image, 1.24× hover-pop 200ms, kFullDuration=420ms) + corner button (36×32 pill) + inline counter (§9.3), ShareBox (row 108px, comment 36-72px appears only after recipient selected, 3-dot menu with upward anchor) (§9.4), DeleteMessagesBox full body-text matrix (4 constructors, all tr::lng_* keys, moderate-panel checkboxes, delete-for-everyone gating) (§9.5), canonical message-menu item ordering — 25+ items in 3 passes + AyuGram's 7 insertions (Edits History, Hide, User's messages, Repeat, Message details submenu, Read until, Burn) (§9.6), upstream-vs-AyuGram delta matrix (§9.7).

**§1–§13 core-UI group is COMPLETE** as of session 2026-04-19. Every gap in §1-§13 has a cited subsection in `research/telegram_desktop_ui.md` with `file:line` source references. Outstanding items are flagged per-section as "needs direct-clone next session" for the few tokens/files that 404 via WebFetch (see §11.9, §12.9, §12.10, §13.6).

- ✅ §54 AyuGram Filters semantics + Shadow Ban flow added (2026-04-19): full Filter Engine Semantics block (ICU 78 regex flavor, synthesized match-input blob, hide-only action, evaluation order, `filtersEnabledInChats` gate, cache lifecycle); AyuFiltersList three-mode screen (Shared / Shadow Ban / Per-dialog) with row click + popup menu spec; RegexEditBox single-filter edit form (5 fields, ICU validation, Save/Cancel); PerDialogFiltersListController (status text formula, click flows for both modes); Shadow Ban storage + peer-menu entry (`AddShadowBanAction` -- chat-list right-click only, NOT message bubble) + Block-vs-ShadowBan semantics; `Ui::FillImportFiltersBox` free function (clipboard / dpaste URL modes, JSON v2 wire format with example, shallow validation). Also corrected misplaced "Shadow Ban" message-menu entry at line 15537.

**§14–§19 Settings group is COMPLETE** as of session 2026-04-19. All six settings sections filled in six separate commits (three per batch, dispatched as parallel Opus agents). Outstanding items flagged per-section as "Unresolved for direct-clone" for the few style-block files that 404 via WebFetch (see §17.11 experimental flags, §18.3.1 `defaultPeerListItem.height` base, §19.16 `langsRadio`/`passportRowPadding`).

**§20–§26 Media / Wizards / Topics / Keyboard / Theming / Admin group is COMPLETE** as of session 2026-04-19. All seven sections filled via parallel Opus agents, committed separately. Key corrections captured in strike-throughs below. Notable: §23-§26 batch pulled 4 agents in one dispatch producing ~450-500 lines each; §25 colorizer clamp and §26 anti-spam config are the most load-bearing spec corrections in this group.

Session-handoff rule: pick any one of the remaining section groups below (§27-§34 auth-export-bots-stories / §35-§49 states-popups-misc / §51-§55 AyuGram extensions), spawn parallel research agents (3+ at a time), and fill in. Each fill should be committed separately for clean history.

---

## §1–§13 — Core UI ✅ COMPLETE

Audit found dark-theme variants for almost every token are missing. §57 appendix now covers the palette — cross-reference it when filling these. All §1–§13 gaps are resolved (see strike-throughs below):

- ~~§1 (line 23): shadow-separator opacity; (line 31) shrink formula <932px; (line 37) Wayland/KDE titlebar rules.~~ **DONE** — Column Shadow Separators subsection, Responsive Breakpoints expanded with shrink formula, Linux Titlebar subsection.
- ~~§2 (line 62): search-focused tabs/chips dimensions; (line 68) stories ring gradient stops; (line 76) unread/mention/reaction badge metrics; (line 86) dark-theme hover/active; (line 91) archived row visual; (line 101) swipe quick action spec.~~ **DONE** — all six gaps filled with source-verified metrics.
- ~~§3 (lines 110, 115, 131, 134, 151): profile area dims, account-switcher rows, menu row metrics, night-mode toggle widget, footer styling.~~ **DONE** — Profile cover dims, Account section (26px avatar + 2px active ring), Menu row metrics (`mainMenuButton`), Night-mode toggle, Footer (two FlatLabels + tooltip).
- ~~§4 (lines 159, 166, 175, 185, 189, 197): topBarBg resolved, avatar size, right-side buttons, pinned-msg bar, action bar, selection mode.~~ **DONE** — all six gaps filled; cited `info.style`, `chat_helpers.style`, `ui/chat/message_bar.cpp`, theme palettes.
- ~~§5 (lines 207, 210, 219, 241, 245, 249, 253): jump-down + mentions buttons, date badge font/bg, bubble colors, info row metrics, peer-name palette hex (cross-ref §57.6), selection checkbox, service message pill radius/opacity.~~ **DONE** — full resolved bubble + service-bg color tables (day/night); JumpDownButton geometry + slide; selection checkbox (20px, 160ms); peer palette confirmed server-fetched for indices 8..63.
- ~~§6 (lines 265, 273, 287, 291, 295, 307, 311, 319): photo spoiler particles, album layouter formula, sticker shadows, voice waveform, round-video arc, polls radio/quiz shake, live-location ring, web preview thresholds.~~ **DONE** — spoiler animation descriptor (3000 particles / 60 frames @ 33ms), full grouped-layout algorithm, fireworks particle counts + colors, quiz shake ±3° linear 400ms, confirmed no YouTube-specific overlay, confirmed no client-side pulsing-dot on live-location maps.
- ~~§7 (lines 327, 333, 343, 349, 365, 369, 373): compose strip dims, attach dropdown, send button anims, voice-record bar, bot keyboard, drag overlay, send-files dialog.~~ **DONE** — seven subsections §7.1-§7.7 added, all source-cited against `chat_helpers.style`, `send_button.cpp`, `history_view_voice_record_bar.cpp`, `history_drag_area.cpp`, `send_files_box.cpp`. Key finding: desktop paperclip opens native OS picker (no styled dropdown) — mode selection happens inside SendFilesBox.
- ~~§8 (lines 408, 409, 411, 420, 431): cover compression math, action button row, shared media buttons, members row, grid columns formula.~~ **DONE** — six subsections §8.1-§8.6 added. Key finding: shared-media has NO horizontal tab strip — navigation is either vertical type-button stack (Saved-Messages view) or full-panel sub-sections via `showSection`. Cover shrink is a 3-snap model (0/112/180px), not continuous interpolation.
- ~~§9 (lines 449, 473, 494, 498, 504): context menu chrome, attention-style color, reaction strip, forward dialog, delete confirmation.~~ **DONE** — seven subsections §9.1-§9.7 added. Key finding: attention items use **red icon, normal text** (full-red-text only in Leave-group confirmation submenus via `menuWithIconsAttention`). AyuGram inserts 7 extras into message context menu (Edits History, Hide, User's messages, Repeat, Message details submenu, Read until, Burn) — all opt-in via `AyuSettings::show*InContextMenu()`. "Copy Info" submenu is downstream uniclient only, not upstream AyuGram.
- ~~§10 (lines 512, 516, 524, 528, 532, 536): panel bg/shadow, tab bar, skin-tone popup, pack footer, GIF masonry, inline emoji dropdown.~~ **DONE** — seven subsections §10.1-§10.7 added. Key resolutions: `emojiPanWidth=345`/`emojiPanMinHeight=278`/`emojiPanMaxHeight=640`/`emojiPanRadius=8`, height-clamp is `emojiPanHeightRatio × windowH` clamped into min/max (NOT "75% fixed"). Auto-hide `kHideTimeoutMs=300`/`kDelayedHideTimeoutMs=3000` (`tabbed_panel.cpp:23-24`). Skin-tone `kColorPickerDelay=500ms` (`emoji_list_widget.cpp:47`). Sticker footer `kVisibleIconsCount=8` + `stickerIconMove=400ms`. GIF layout corrected: line-packing via `Ui::Mosaic`, NOT true masonry. Inline suggestions = `field_autocomplete` (separate widget), rows = `mentionHeight=40`, 4.5-row cap ~180px, inline-bot `kInlineBotRequestDelay=400ms`.
- ~~§11 (lines 550, 556, 564, 572, 582, 588, 594): _next button states, QR code quiet zone, country picker, CodeInput cells, 2FA error color, reg avatar picker, cover gradient.~~ **DONE** — nine subsections §11.1-§11.9 added. Key: `introNextButton 300×42/6r` at `introNextTop=266`, slide-in from `+introNextSlide=200px` via `anim::interpolate`. QR `introQrMaxSize=180`, `introQrBackgroundSkip=12` (NOT 8 — earlier spec was wrong), `introQrCenterSize=44`, logo `#40A7E3`. Code cells: `kWidthRatio=0.8`, `kSlideDistanceRatio=0.2` → 10px entry slide, 20px font. Cover `introCoverHeight=208`, `introCoverDuration=200ms`, easing `easeOutCirc` only when `hasCover()` else linear. §11.9 flags 4 unresolved files (input_field.cpp shake, country_select_box, palette hex, userpic_button) for next-session direct-clone pass.
- ~~§12 (lines 604, 610, 612, 623, 630, 635, 642, 646, 650): panel size, signal bars, encryption fingerprint, PIP drag, narrow/wide transition, blob ring formula, TopBar gradients, screen-share chooser, rating dialog.~~ **DONE** — ten subsections §12.1-§12.10 added. Key: `callWidth×callHeight=720×540`, min `380×520`. Signal bars 4-count, heights `4/6/8/10` formula-derived, `inactiveOpacity=0.5`. Fingerprint: `kEmojiInFingerprint=4` shown, `kEmojiInCarousel=10` during reveal, `kStartTimeShift=50ms` stagger, `kCarouselOneDuration=100ms` → total reveal ~1200ms, `kTooltipShowTimeoutMs=1000ms`. PIP 160×110, `DragMode::SnapToCorners` default `BottomRight`, nearest-corner Euclidean on release. **Corrects spec: `groupCallWideModeWidthMin=600` NOT 960**. Blob dual-system: minor 6-vert/0.545 scale, major 8-vert/0.605, min/max radius `27/29px`, envelope `kLevelDuration=215ms`, userpic pulse `0.8→1.0`. TopBar `callBarHeight=38`, 28px userpics, 4 gradient states (Active/Muted/ForceMuted/Connecting) with token names. §12.9/§12.10 flag `calls_choose_source_box.cpp` + `calls_rate_call_box.cpp` as 404 — need direct-clone next session.
- ~~§13 (lines 656, 669, 676, 683): OneColumn slide, narrow-width thresholds, touch long-press ms, web visual diffs.~~ **DONE** — six subsections §13.1-§13.6 added. Resolved window.style tokens: `columnMinimalWidthLeft=260`/`Main=380`/`Third=292`/`adaptiveChatWideWidth=880`. Corrects spec: OneColumn switch = `left+main=640px`, ThreeColumn = `left+main+third=932px` — derived from tokens, NOT hardcoded literals. Calls 600px threshold is distinct (see §12.5). Touch: `kStartReorderThreshold=30`, `kStartDragToFilterX=30/Y=75`, `kFreezeTimeout=2000ms`; long-press = `QApplication::startDragTime()` (platform-delegated, not Telegram-specific). Corrects spec: "avatar-only" dialog mode does NOT exist — sidebar is clamped at 260px via `adjustColumnsToFit()`. Flags `slideDuration`/`dialogsWidthDuration`/`shakeDuration`/`slideWrapDuration` numeric values (live in style headers not retrievable via WebFetch).

## §14–§19 — Settings

- ~~§14: cover height formula, nav row metrics, scale slider, bio counter, profile rows, accounts list, theme cards, cloud themes, stickers/messages subsections, advanced, premium/help footer.~~ **DONE 2026-04-19** — §14.2.1 cover formula + photo proportions, §14.2.2 scroll/collapse, §14.2.3 cover right-side controls (QR + badge), §14.3.1 nav row metrics, §14.4.1 scale slider (5% step, restart semantics, preview tooltip), §14.5.2.1 bio counter (70/140 char cap + red-below-zero threshold), §14.5.3.1 profile info row layout, §14.5.6.1 accounts list (row dims, badge, reorder, context menu, Add-Account gating), §14.6.1.1 theme card dims (80×92 preview, 40×14 bubble, fit algorithm), §14.6.3.1 cloud themes (Edit Current Theme, share flow), §14.6.6.1/§14.6.7.1 Stickers and Emoji / Messages control maps, §14.7.0 Advanced build order (11 builders with conditionals), §14.7.3/§14.7.5/§14.7.6 Window Title / System Integration / Performance ordered row sequences, §14.8.1 Premium footer (5 buttons + balance bindings), §14.8.2 Help section (3 rows + Ask-a-Question confirm). **Unresolved for direct-clone:** exact `settingsBioCountdown` style block, `premiumButton` gradient definition, AyuGram-owned `ayu/settings/` footer hooks.
- ~~§15: volume slider label, per-type separator, tone row, ringtones box, monitor widget.~~ **DONE 2026-04-19** — §15.2.1 global master-volume slider (100 steps, `"N%"` label, live-preview, settingsBigScalePadding), §15.4.1 intra-row separator (1px `lineWidth` vertical hairline, `textBgOver`, NO horizontal divider between per-type rows), §15.5.1.1 tone row (navigation row NOT dropdown, menuIconSoundOn, right-aligned filename via `ExtractRingtoneName`, "Default" fallback, no play-on-select), §15.5.1.2 per-type volume slider (preview plays per-type tone), §15.6.1 ringtones box (kDefaultValue=-1/kNoSoundValue=-2, play-on-click, right-click delete, 100 KB / 100 saved / 5 s server limits, AudioCreator 250 ms detach poll), §15.11.1.1 Windows-only Focus Mode (SkipToastForCustom gating), §15.11.2.1 multi-display selector (Radiobutton group NOT drawn monitors, `"{name} (W×H)"` label with `\u00D7`, checksum persistence), §15.11.3.1 monitor widget (280×160 screen rect, notificationsBoxMonitor icon, 64×16 samples, 3×3 hit grid, BottomRight default, TopCenter-as-left quirk), §15.11.4.1 notification count slider (5-section `Ui::SettingsSlider`, default 3). **Unresolved for direct-clone:** Linux dbus focus-inhibit hook, per-OS `Platform::ScreenDisplayLabel` differences.
- ~~§16: cloud-password field widths, TTL radio, AutoLockBox, active sessions (de-dup with §19), EditPrivacyBox, forward preview bubble, Charge Stars slider, gifts toggles, ClearPaymentInfoBox, file-extensions input, SelfDestructionBox, blocked users empty state.~~ **DONE 2026-04-19** — §16.2.1.1 cloud-password box (PasscodeBox widths, label/helper fonts, error ring, recovery-email row), §16.2.2.1 TTL radio (preset seconds, row style, selected ring, info footer), §16.2.3.1 AutoLockBox (box width, preset timeouts, custom TimeInput positioning), §16.2.6.1 blocked users empty state (Lottie name, title/description keys, min height, "Block user" row), §16.2.7.1 sessions list row layout (cross-ref §19, current-session highlight, terminate-all confirm, auto-terminate row), §16.3.0 EditPrivacyBox shared internals (box width, exception link row, exception-list preview, save-button), §16.3.4.1 forward preview bubble (bubble sizing, "Forwarded from X" label, per-option tooltip), §16.3.7.1 Charge-Stars slider (min/max, step scheme, commission/USD, "Remove fee for"), §16.3.9.1 gifts toggles (5 accepted-type toggles + "Show gift button in input"), §16.5.1 ClearPaymentInfoBox (2 checkboxes, danger Clear button), §16.6.1 file-extensions input (multi-line, 10240-char / 1024-entry caps), §16.8.1 SelfDestructionBox (account vs sessions radios in days, info strings). De-dup with §19 noted via cross-reference rather than duplicated body.
- ~~§17: ProxiesBox full, LocalStorageBox tick labels, AutoDownloadBox width, Window Title checkboxes, Linux close-behavior, system integration, PowerSavingBox, ANGLE/OpenGL restart, settingsUpdateToggle progress, experimental section.~~ **DONE 2026-04-19** — §17.2.1.1 ProxiesBox full layout (width + empty state), §17.2.1.2 ProxyRow geometry, §17.2.1.3 connection-test icon/text state table (5 states), §17.2.1.4 context menu + keyboard shortcuts, §17.2.3.1 LocalStorageBox slider tick labels/ranges (18/18/16 positions), §17.2.3.2 summary row + per-tag row (6 tags), §17.3.1 AutoDownloadBox per-source layout (NO network tabs in upstream — confirmed), §17.4 Window Title rewritten — correct 3 checkboxes + SlideWrap gate on account-name; "system/Qt window frame" moved to §17.6, §17.5 Linux close behavior (enum labels + tray-availability gate), §17.6.1-§17.6.4 tray/taskbar/monochrome/autostart/native-notifications (platform gates + passcode block), §17.7.1.1 PowerSavingBox (5 category groups), §17.7.3 ANGLE backend 5-option dropdown + restart dialog, §17.7.4 OpenGL toggle + restart dialog, §17.10.1-§17.10.3 updater state label matrix + progress + transitions (auto-retry, no user "Retry"), §17.11.1-§17.11.3 experimental section layout + flag list + import/export (base64url + qCompress + JSON).
- ~~§18: filter divider label font, FilterRowButton timings, icon-toggle anchoring, FilterChatsPreview scroll, color buttons, inviteLinkList row, icon picker panel, disabled row status, tab view threshold.~~ **DONE 2026-04-19** — §18.2.1 divider label typography (`settingsFilterDividerLabel` minWidth 200, 13px `normalFont`, `windowSubTextFg`, 0/16/0/22 margins; Recommended section uses plain `AddDivider` + `defaultSubsectionTitle`), §18.3.1 FilterRowButton metrics + timings (ripple style, no hover-fade, instant icon tint swap, color dot = `height/3`, tag fade `universalDuration=120ms`, removed-opacity 0.4, settings page does NOT install reorder controller), §18.6.2.1 chat-type toggle anchoring (44px row, 34px photo circle @13/5, name @59/14, centered icon, 10px right remove), §18.6.2.2 FilterChatsPreview (corrected — vertical stack NOT horizontal strip, no `+N` overflow, no cap, empty = 0-height), §18.6.4.1 color chips (30px `userpicBuilderEmojiAccentColorSize`, single-row wrap-less, 120ms crossfade, theme-indexed palette — do NOT hard-code hex; chip 7 = archive-grey + white ×), §18.6.5.1 inviteLinkList row (52px, 44px icon @9/4, name @60/6, status @60/26, 3-dots 12px right), §18.8.1 icon picker panel (6×5 grid, 44×42 cells, margins 10/36/10/8, no emoji fallback/search/"Use emoji instead" link here — emoji lives on name-field button, 300ms hide-after-leave), §18.11.1 disabled-row status (no inline disabled state upstream; AyuGram regex gates are global toggles in a separate screen), §18.12.1 tab threshold = `windowMinWidth(380) + windowFiltersWidth(72) = ≥452px` falls back to horizontal scroll, reorder uses `QApplication::startDragDistance()` + `slideWrapDuration=150ms`. **Unresolved for direct-clone:** `defaultPeerListItem.height` base (in lib_ui peer-list style), color chip hex lookup (`ui/empty_userpic.cpp`).
- ~~§19: Rename style, current-session gradient, other-sessions dividers, SessionInfoBox fields, Rename dialog, SelfDestructionBox radio, power-saving indent, automatic-mode overlay, LanguageBox translation toggles, MultiSelect, language row height.~~ **DONE 2026-04-19** — §19.2.1 Rename style (`defaultLinkButton`, i18n key, 23px right offset → RenameBox), §19.2.2 current-session "gradient" (corrected — no card gradient; shared `sessionListItem`; only the 42px userpic has a vertical `QLinearGradient(0,0,0,size)` with device-type color pairs table), §19.4.1 other-sessions dividers (corrected — no between-row hairlines; section separation via `AddDivider`/`AddDividerText`), §19.6.1 SessionInfoBox actual AyuGram field set (Application, System, Official App, IP, Location) with i18n keys; legacy fields (secret chats/calls/first-login/active-apps) NOT rendered in current fork, §19.8.1 Rename dialog (GenericBox with `settingsDeviceName` input, placeholder `Platform::DeviceModelPretty()`, `kMaxDeviceModelLength=32`, empty-input revert), §19.9.1 SelfDestructionBox Sessions variant (cross-ref §16.8.1) — TTL diff table, i18n keys, API calls, no info label in Sessions variant, §19.10.1 power-saving indent (standalone GenericBox, no extra indent), §19.12.1 automatic-mode overlay (disabler widget, boxBg alpha 96/255, geometry via `rpl::combine`, `lng_settings_power_turn_off`, 3000ms duration), §19.14.1/§19.14.2 LanguageBox translation toggles + ChooseLanguageBox skip-languages editor (checkbox multi-select, 1-language minimum, `lng_translate_settings_one` toast), §19.15.1/§19.15.2 MultiSelect (clarified LanguageBox uses only search-field aspect — no chips; full `Ui::MultiSelect` reference with chip/padding/animation/scroll metrics), §19.16.1 language row height (formula from `language_box.cpp:553-560`, ≈54px default). **Unresolved for direct-clone:** exact `langsRadio` diameter + `passportRowPadding` numeric values (live in `ui/ui.style`).

## §20–§26 — Media / Wizards / Topics / Keyboard / Theming / Admin

- ~~§20: HiDPI matrix, toolbar more-menu contents, PiP snapping rules, group thumbs overflow, context menu structured table, geometry animation curve.~~ **DONE 2026-04-19** — §20.4.1 HiDPI matrix (DPR stamping on `_docRectImage` + static-image buffers, DPR-before-transform ordering, 4096px texture ceiling), §20.8.1 toolbar more-menu (18-item table sharing `fillContextMenuActions()` with context menu, every item cited with guard-line, no per-item keyboard bindings in dropdown — global shortcuts in §20.18), §20.13.1 PiP (default top-left, **edge-snap NOT corner-snap** via `ClampToEdges()`, `pipBorderSkip: 20px` × 3 + `pipBorderSnapArea: 16px`, anim `easeOutCirc` with `slideWrapDuration`, Qt window-flag z-order), §20.14.1 group thumbs (centered-NOT-scrolled model, `ComputeThumbsLimit()` formula, 150ms `kThumbDuration`, no ring stroke — width differential is only highlight), §20.16.1 context menu structured matrix (18-row × content-type columns — own msg / channel admin / photo / video / story — per-line gating; Rotate is toolbar-only NOT menu), §20.19.1 geometry animation (`_stateAnimation` + `_geometryAnimation` interpolator, show/hide 200/600ms `anim::linear`, shortest-path rotation wrap). **Unresolved for direct-clone:** `widgetFadeDuration` numeric — lives in shared `basic.style`.
- ~~§21: Forum entry flow, userpic icon origin, username validation debounce/API, PublicLinksLimitBox, group toggles visuals.~~ **DONE 2026-04-19** — §21.2.1 default userpic origin (72px frame via `st::defaultUserpicButton`, ellipse vs forum-rounded via `ForumUserpicRadiusMultiplier`, 8 `historyPeerNUserpicBg/Bg2` gradient pairs, 2-letter via `EmptyUserpic::fillString`, 28px font `(size*13)/33`, change-icon @21/23), §21.2.2 Forum-entry "Enable Topics" (**lives in Manage-Group NOT Step-1**, `st::manageGroupTopicsButton` + `menuIconTopics` + NEW badge, 200-member gate via `forum_upgrade_participants_min`, API `MTPchannels_ToggleForum(forum, forumTabs)`, forces `hiddenPreHistory=false`), §21.4.1 username validation (debounce `kUsernameCheckTimeout=200ms`, API `MTPchannels_CheckUsername` **channels-scoped NOT contacts**, success = green text NO animated tick, reserved folds into "invalid", no Fragment link in channel box), §21.4.2 PublicLinksLimitBox (free 10 / Premium 20, `premiumIconLinks` bubble, per-row revoke via `MTPchannels_UpdateUsername("")` + `MTPchannels_DeactivateAllUsernames`, CTA → `Settings::ShowPremium(..., "channels_public")`), §21.5.1 group permission-toggle visuals (shared `Settings::AddButtonWithIcon` + `ToggleView` anatomy, slow-mode = `Ui::MediaSlider` pseudo-discrete 8 stops `0/5/10/30/60/300/900/3600s`, Topics nested via `SlideWrap`, Approve-New-Members nested under Only-Members at 42px indent).
- ~~§22: topic icon SVG shape, z-order/stacking, TopicsView separators, context menu conditional matrix, EditForumTopicBox icon panel.~~ **DONE 2026-04-19** — §22.2.1 topic icon SVG geometry (84×84 viewBox speech-bubble with tail, 2.94736842px stroke, per-color hardcoded gradients, `evenodd` fill-rule, DPR-aware rendering, no corner radius), §22.2.2 z-order/stacking three-path model (default = SVG bg → white letter overlay; custom-emoji = emoji replaces bubble entirely with no color bg; general = colorized hash only; `forumTopicIconPosition=(2,0)`, narrow-mode centering formula), §22.3.1 TopicsView separators (**no inter-row dividers in PaintRow** — differentiation by `dialogsBg`/`dialogsBgOver`/`dialogsBgActive` fills + ripple; horizontal title gap `topicsSkip=8px` / `topicsSkipBig=14px` after jump), §22.8.1 context menu matrix (full `fillContextMenuActions` ordered table at `window_peer_menu.cpp:1743-1765`, per-item gating Y/N across admin × creator × non-creator × General × closed × pinned; Edit/Copy-Link reachable only from profile menu), §22.5.1 EditForumTopicBox icon panel (sentinel `kDefaultIconId=0x7FFFFFFFFFFFFFFF`, paint chain with `EmojiFlyAnimation`, Premium gating, color-cycle via `ChooseNextColorId`, keep-emoji vs replace logic, Save gating with `f_icon_emoji_id` suppression for General + local-creating fast path). **Note on paths:** forum icon rendering lives in `data/data_forum_topic.cpp` (not `ui/chat/forum_topic_icon.cpp`); topics view is `dialogs_topics_view.cpp`.
- ~~§23: RTL layout, repeat-period menu visuals, processing-video tip shape, published toast exact px, silent indicator tooltip.~~ **DONE 2026-04-19** — §23.12 Gap Fills (5 subsections, ~219 lines) resolved: RTL mirrored via Qt layoutDirection (no bespoke mirroring), repeat-period menu uses shared PopupMenu with menuIconSchedule, processing-video tip = Ui::ImportantTooltip with rounded pill and shadow, published toast = Ui::Toast with exact padding/font citations, silent indicator tooltip = Ui::FlatLabel inside PopupMenu anchor-less offset.
- ~~§24: account switching defaults contradiction, folder/pinned precedence, Ctrl+Tab overlay spec (critical), markdown table dialog UIs, support-mode visuals, settings UI dimension table.~~ **DONE 2026-04-19** — §24.12 Gap Fills (6 subsections, ~250 lines) resolved: Ctrl+Tab **uses built-in account-switcher filter cycling NOT a separate overlay widget** (critical correction — no custom overlay exists), account-switch default = next active session by order, folder/pinned precedence = `kShortcutFolderFirst=true` gates whether Ctrl+1 jumps to folder[0] or pinned[0], markdown dialog = standard Ui::InputField with error ring, support-mode chrome = admin badge + telegramDesktop peer-color slot, settings UI dimension reference table cross-refs §14-§19.
- ~~§25: full palette dump (reference §57), colorizer Night clamp, colorize exclusion list, theme editor px, pattern intensity math, `ThemeAdjustedColor` sampling, per-chat theme chooser px, CloudListCheck dims.~~ **DONE 2026-04-19** — §25.17 Gap Fills (8 subsections, ~393 lines) + 2 inline pixel-dims table fixes. Key corrections: **colorize exclusion list is 63 tokens not ~81**, **lightness clamp is asymmetric `Day [0,160]` vs `Night [64,255]`** (prior spec said `[64,160]` for both — wrong), **pattern default intensity = 50** (not 75/100), `ThemeAdjustedColor` samples via HSL with fixed 0.65 blend, per-chat theme chooser = 80×92 CloudListCheck tiles with 40×14 preview bubble + 3px ring on selected, theme editor uses 4 input rows (Name/Pattern URL/Intensity/Blur) + live preview panel.
- ~~§26: Direct-Messages star input UI, permission dependency graph, ManageDirect definition, transfer ownership steps, admin log quoted bubble, admin log event table, progress arc intervals, invite link pickers, empty-state roles, pixel dims table resolved, slowmode send-button countdown, anti-spam threshold.~~ **DONE 2026-04-19** — §26.12 Gap Fills (12 subsections, 438 lines). Key corrections: **no standalone ManagePeerBox/ManageDirectBox** (ManageDirect is a ChatAdminRights flag, not a screen); **admin log has NO loading spinner** (confirmed); transfer-ownership = 7-step dry-run error-dispatch flow (not linear wizard); anti-spam threshold = server config `appConfig[telegram_antispam_group_size_min]` default 100; 51 admin log event types enumerated; Direct-Messages star input = MediaSliderWheelless with `kDefaultPrivateMessagesPrice=10`; slowmode countdown = centered mm:ss text REPLACING send icon; invite-link picker = `kPeriod=30d`; permission dependency — Stickers/GIFs/Games/Inline form mutual equivalence class; empty states reuse `lng_blocked_list_not_found` (no Lottie).

## §27–§34 — Auth / Export / Bots / Stories / Contacts / Calls

- ~~§27 (4794–4940): per-platform biometric APIs, multi-account lock layout, AutoLockBox custom input.~~ **DONE 2026-04-19** — §27.14 Per-Platform Biometric (honest gap: Linux no binding; AyuGram delegates to upstream base/system_unlock.h), §27.15 Multi-Account Lock Layout (single global lock, N windows, log-out link, auto-wipe on last logout), §27.16 AutoLockBox (Ui::TimeInput HH:MM, kCustom sentinel, style keys resolved, validation + save path). Old §27.14 pixel summary renumbered to §27.17.
- ~~§28 (5170–5621): Add*Field wrapper error ring, SentCodeField cells, Ui::CodeInput email, EmailConfirm step stack.~~ **DONE 2026-04-19** — §28.2.3a Add*Field wrapper error ring (Ui::InputField border state table, AddError auto-hide, AddLinkButton docking at passcodeTextLine=28px); §28.3.5a Ui::SentCodeField internals (single-field, digit-only filter via fix() pipeline, auto-submit hook); §28.3.5b CloudPasswordEmailConfirm full walkthrough (setupContent build order, setup-vs-recovery branches, button-state table, stack call-order); §28.9.1a Ui::CodeInput cell widget (40×50px cells, 4px border, windowActiveTextFg focus, shake on error, auto-submit via codeCollected).
- ~~§29 (5730–6017): Account Data checkbox indent, ChooseTimeWidget dims, progress row fonts, TAKEOUT_INVALID box.~~ **DONE 2026-04-19** — §29.3.6 Account Data checkbox indent (exportSettingPadding flat layout, addOption/addOptionWithAbout helpers, option-order table, contrast with 56px sub-option indent); §29.4.1.1 ChooseTimeWidget box layout (GenericBox, hiddenDaysInput=true → 2 fields, no separator glyph, 600s kOffset); §29.5.2.1 Progress row fonts & paint (14px semibold label, windowSubTextFg info, 3px bar with sineInOut, 200ms cross-fade, no ETA); §29.8.1.1 TAKEOUT_INVALID box (Ui::MakeInformBox, escape+outside disabled, distinguished from DELAY_N and Disk/IO error paths).
- ~~§30 (6127–6484): bot menu button width formula, Web App loading screen, game button states, Login URL Auth dialog, payments panel details.~~ **DONE 2026-04-19** — §30 Bot Interactions: bot menu button width `clamp(labelWidth + 48, 30, 160)` with ellipsize, historyBotMenuButton style, 120ms universalDuration; Web App loading screen (paymentsLoading 24×24/4px stroke windowSubTextFg, NO Lottie on desktop); Game Play 4-state machine (idle/hover/press/loading) with RippleAnimation BubbleCornerRounding::Large bottom, 14×14 historyLoadingRadial; UrlAuthBox (320px, 64×64 userpic, dual-checkbox disable dependency, phone sub-box, 4-emoji match codes 48×48); Payments panel (PAY $X labels, plain-text provider branding — no Stripe/Smart Glocal logos, tip 10%/80% opacity states, 6-field shipping form).
- ~~§31 (6679–6789): SavedMessagesTagBar widget, EditTagNameBox, Subsection Tabs width computation.~~ **DONE 2026-04-19** — §31.7a SavedMessagesTagBar (Dialogs::SearchTags, not a QWidget — paints into parent; Tag struct QRect, reactionInline* tokens 18px height/32px emoji/12px font, shift-click multi-select, MakePromoLink flow, HONEST GAP re: lookupHandler); §31.7b EditTagNameBox (inline EditTagBox free function at history_view_context_menu.cpp:1627-1717, kTagNameLimit=12 setMaxLength(24), inline emoji preview, empty-submit clears); §31.9a Subsection Tabs width `strictSkip + _text.maxWidth() + badge + padding`, chatTabsSlider (36/18/33px) / chatTabsVertical (64/50/28/54px), wheel-Y→X redirect, half-tab-peek scroll, `scrollMax ≤ 3 × viewport` doubles limit (12 → 24 → 48).
- §32 (7076–7405): Public badge, reply compose layout, views list menu, profile stories grid, ~~story creation editor~~ (**DONE** — §32.15 rewritten), reaction panel trigger.
- ~~§33 (7528–7682): stories bar in contacts, country code picker, edit-contact cover overlay.~~ **DONE 2026-04-19** — §33 Contacts: Stories Row Widget per-row PeerListStories ring (NOT horizontal strip inside Contacts box) with dialogsStories/dialogsStoriesFull tokens (21/42px photos, 3/4px lineTwice, UnreadStoryOutlineGradient, red for video streams, readOpacity=0.6); Country Code Picker CountrySelectBox (320px boxWidth, Ui::MultiSelect search, text-only rows — HONEST GAP re: mobile flag convention, Type::Phones vs Type::Countries); Edit-Contact Cover Overlay Cover class (Ui::FixedHeightWidget 108px, infoEditContactCover 72×72 avatar @ 19/18, name @ 109/33, status @ 109/57 — HONEST GAPs: NO gradient background, NO badges, contrasts with Profile cover §13).
- ~~§34 (7895–8099): date-group headers, Rate Call dialog, active-call top bar details, ~~Group::PrepareCreateCallBox~~ (**DONE** — §34.17 added).~~ **DONE 2026-04-19** — §34 Calls History: Date-Group Headers Investigation Result NONE (HONEST FINDING — AyuGram has no date-group headers; dates inline per-row via lng_call_box_status_today/yesterday/date; tokens recommended if deviating); §34.13.1 Rate Call Dialog DEAD-CODED in calls_call.cpp:1112 `if (false && data.is_need_rating())`; full RateCallBox spec (boxWideWidth=364px, 5 IconButton stars 36×36, InputField lazily created/destroyed at 5 stars, kRateCallCommentLengthMax=200, MTPphone_SetCallRating flow); §34.15.1-9 Active-Call Top Bar (Calls::TopBar in MainWidget SlideWrap, 6-element horizontal layout mute/duration/signalBars/infoLabel/hangupLabel/hangup, 4-state gradient Connecting/Active/Muted/ForceMuted via anim::gradient_colors 150ms interrupt-safe, 3-layer group call blobs 5/7/8 segments, kBlobUpdateInterval=100ms, kHideBlobsDuration=500ms).

## §35–§49 — States / Popups / Misc

- ~~§35 (15325–16044): universal "resolve constants to px" for every subsection; skeleton row, shared-media empty, connection state widget.~~ **DONE 2026-04-19** — §35 Empty/Error/Loading: resolved all dialogs.style empty tokens (`dialogEmptyButtonSkip=12px`, `recentPeersEmptySize=100px`, `recentPeersEmptyHeightMin=220px`, `recentPeersEmptyMargin=10px all`, `recentPeersEmptySkip=10px`); `info.style:1596–1609` shared-media empty tokens (`infoEmptyIconTop=120px`, `infoEmptyLabelTop=40px`, `infoEmptyLabelSkip=20px`, `infoEmptyLabel.minWidth=220px`, icon centred at 1/3 height, label `newWidth-40px`); `window.style:140–157` connection-pill geometry (`connectingMargin=2px`, `connectingTextPadding=18/11/18/0`, `connectingRadial=20×20 thickness 2px menuIconFg`, `connectingRetryLink padding=6/11/6/0`, `connectingDuration=150ms`, `kConnectingStateDelay=1000ms`, `kMinimalWaitingStateDuration=4000ms`, bottom-left anchored, proxy icon right-anchored vertically centred); `chat.style` chat tokens (`msgServiceFont=semiboldFont`, `msgServicePadding=12/3/12/4`, `msgPadding=11/8/11/8`, `msgFileRadialLine=3px`, `historyAudioRadialLine=2px`, `historyGroupAboutPadding=24/16/24/16`, bullet/header/text/skip 16/10/10/8, `chatIntroStickerSize=96px`, `chatIntroWidth=224px`, `chatIntroTitleMargin=11/16/11/4`); skeleton-animation constants (`kSlideDuration=1000ms`, `kWaitDuration=1000ms`, `kBaseAlpha=0.5`, `kGradientAlpha=0.2`, thickness=font.height/2, radius=thickness/2); dialog-row skeleton (avatar=st.photoSize, name 60×semiboldAscent, status 100×defaultAscent randomised `width/4+rand(width/2)`, glare 1s+1s cycle, RTL mirrored). HONEST UNRESOLVED for next session: literal hex of all palette tokens (`colors.palette` lives in lib_ui submodule — direct-clone next session); `defaultPeerListAbout` style block; native PNG height of `connecting_left/body/right.png` (estimated ≈30 px); exact `kQueryPreviewLimit` constant (commonly 18 upstream). Patch written to `/tmp/section_35_additions.md`.
- ~~§36 (16045–16640): box scale/easing, color picker swatches, toast style, popup menu dims, tooltip arrow, calendar/time picker.~~ **DONE 2026-04-19** — §36 Common Dialogs: box show/hide is **200 ms easeOutCirc opacity-only** (no scale tween in current `lib_ui` source — corrected the "97%→100% scale" prose); `boxRadius=8px` (corrected from 6px); `defaultPopupMenu` resolved (radius 8, show 200/hide 150 ms, `defaultPanelAnimation` clip-reveal w0.5/h0.3/o0.2, item padding 17/8/17/7, width 156–300 px, separator 1 px); `defaultToast` resolved (padding 19/13/19/12, radius 6, fade-in 200/fade-out 1000, slide 160 ms, max 480 px; multiline 160–360 px); `defaultTooltip` (padding 5/2/5/2, max 800 px × 12 lines, show 1000 ms, hide-by-leave 10 ms); `defaultImportantTooltip` arrow **8×4 px triangle**, radius 4 px, 200 ms; CalendarBox cells **48×40 px** (inner 34), title 48 px, days header 40 px; ChooseDateTimeBox `scheduleHeight=95 px`, date 136 px / time 72 px / "at" skip 24 px. HONEST UNRESOLVED for next session: `colorPicker*`/`colorSlider*`/`colorSample*`/`colorEdit*`/`colorField*` tokens (style file not located via WebFetch — needs direct AyuGram clone), `historyMessagesTTLPickerHeight/ItemHeight`, `defaultToastIconPadding`, `roundShadowRadius8px` numeric extend/blur/opacity, `layerBg` alpha, residual box scale tween (delegate path).
- ~~§37 (16641–17147): sizing table px, reply input grow rules, hidden userpic placeholder, stack overflow rules.~~ **DONE 2026-04-19** — sizing table fully resolved from `window.style:18-67` (notifyWidth=320/MinHeight=80/PhotoSize=62/etc); reply InputField clamps 36-72px → popup grows to 117-153px total; HideAll placeholder = `LogoNoMargin()` scaled 62×62 (square, NOT circular); kMaxNotificationsCount=5 / default=3 (corrected from 5); stack overflow = sticky-aware FIFO with no client-side scroll. 5 honest gaps flagged (palette hex, `defaultTextStyle` line height, HideAllButton .style block).
- §38 — complete.
- ~~§39 (10153–10279): button-bar spacing, blur debounce, scroll-to-zoom rule.~~ **DONE 2026-04-19** — §39 Photo Cropping: content margins 20/20/20/146 px (photoEditorContentMargins editor.style:9-12); resize handles 10×10 corners (photoEditorCropPointSize), min crop 20 px; §39.5 Zoom Controls confirmed NONE (no wheelEvent in photo_editor_content.cpp or editor_crop.cpp, no QGestureEvent/pinch, no kbd zoom — direct-clones MUST NOT add scroll-zoom); §39.11 Transform-mode bottom bar full geometry (bar 48×422 px, ButtonBar layout algorithm); §39.14 Background blur+debounce (×4 scale, 24 px blur, dim 16,16,16,192/128, fast 200ms / full 1000ms / cross-fade 200ms); §39.14a-b cheat strip with 28 entries. HONEST UNRESOLVED: photoCropFadeBg/PointFg palette tokens (likely mediaview.style); st::lineWidth/semiboldFont/dialogsTextFont (global ui/basic.style); slideWrapDuration default; UserpicButton._st.duration depends on caller.
- ~~§40 (10319–10465): shrink scale factor, SingleFilePreview icons, HD badge dims, album layout constants, drag-area cross-ref.~~ **DONE 2026-04-19** — §40 Send Files: shrink "scale factor" CORRECTED to 5 px LINEAR inset (not multiplicative scale) = ceil(roundRadiusLarge/1.4); HD badge full geometry from PaintHighQualityBadge (text/font/xpadding=2/ypadding=0/stroke=1/radius=h/3, photoQualityBadgeOuterSkip=3 px); SingleFilePreview CORRECTED (was 48×48, actually two layouts: attachPreviewLayout 44 px circle for icon mode + attachPreviewThumbLayout 64 px square for raster thumb); album layout constants table (kMaxAlbumCount=10, sendMediaPreviewSize=308, historyGroupWidthMin=minPhotoSize=100, historyGroupSkip=4, ComplexLayouter ratio bounds 0.6667-2.75, classification thresholds >1.2 wide/<0.8 narrow); size limits (kFileSizeLimit=2000MiB, kFileSizePremiumLimit=4000MiB, PhotoSideLimit standard=1280/HD=2560 — flagged kStandardPhotoSideLimit doesn't exist in source); §40.13 drag-area refactored to defer to §48. HONEST UNRESOLVED: historyFileThumbPlay icon px, historyFileInIconFg/iconBg palette colours (defer §57), SpoilerAnimation tick rate, SetupShadowsToScrollContent shadow heights.
- ~~§41 (10838–10951): EditLinkBox widths, EditCodeLanguageBox length, blockquote bar.~~ **DONE 2026-04-19** — §41 Formatting Toolbar: §41.6 EditLinkBox box width 320 px, inner field 276 px (boxWidth − 2×22 markdownLinkFieldPadding), text field defaultInputField (heightMin 55/heightMax 148/border 1·2 px/placeholder duration 150ms), URL field AttachParentChild on empty placeholder for free positioning; cite-range corrected (138-282 not 138-230); §41.7 EditCodeLanguageBox kCodeLanguageLimit=32, regex `^[a-zA-Z0-9\+\-]*$`, settingsAddReplyField (heightMin 36/transparent/32 px right textMargins for counter), settingsAddReplyLabel minWidth 256 px; §41.13.1 Blockquote & Pre Bar fully resolved from historyQuoteStyle (outline 3 px LEFT RULE, radius 5 px, padding margins(10,2,4,2), verticalSkip 4, outlineShift 2); blockquote adds 20 px right padding for mini_quote, pre adds 20 px header strip with mini_copy + scrollable=true; same dims apply in compose. HONEST UNRESOLVED: compose-field kTagPre header click-target rect, AddLengthLimitLabel style, palette hex for msgIn/OutBgQuote/Code (defer §57).
- ~~§42 (11017–11116): tab pill dims, tab bar wrapping, right emoji px.~~ **DONE 2026-04-19** — §42 Reactions Detail Popup: §42.4 Tab Bar pill geometry resolved (height 32 / radius 16 = h/2 / padding 6+12 / font 13 px semibold from MultiSelectItem widgets.style:649; container outer padding (12,10,12,10) from reactionsTabs chat.style:1080; itemSkip 8 px GAP not part of row; selection-transition 150 ms from MultiSelectItem.duration; wrap behaviour confirmed via reactions_tabs.cpp:106-111 — wraps on overflow, NEVER scrolls); §42.5 User List CORRECTED: Mode B uses infoMembersList (photoSize 46 px / namePosition (79,11) / row 58 px from defaultPeerListItem widgets.style:1416) — previous draft cited defaultWhoRead's 30 px / 57 px which are Mode-A values; right-action emoji = GetSizeNormal()/DPR = 18 logical px with margins L9/T20/R27/B0 within 58 px row; §42.12 Sizing resolved (infoDesiredWidth 392, infoMinimalLayerMargin 48, infoLayerTopMin/Max 20/40, Mode A 40 px entry / 30 px summary, Mode B tabs formula 10+N×32+(N-1)×8+10). HONEST UNRESOLVED: AdjustCustomEmojiSize(18) exact rounded value at fractional DPRs, st::semiboldFont exact px (project convention 13 px), st::whoReadMenu widthMin/widthMax, st::slideDuration numeric value, reactionsTabAll/Played/Checks SVG intrinsic dimensions.
- ~~§43 (10659–10681): sizing px, date-line icons.~~ **DONE 2026-04-19** — §43 Read Receipts Detail: §43.4 User List geometry fully resolved (row 40 px = photoSkip×2+photoSize=5×2+30; photo 30 px @ left 13 / top 5; nameLeft 57; whoReadNameWithDateTop 3; whoReadDateTop 20; whoReadDateSkip 15; whoReadDateChecksPosition (-7,-4); itemPadding.right 17; preloader α 0.2); §43.4.2 Date-Line Icons table from .cpp:558-573 switch (Viewed=read_ticks_s/windowSubTextFg, Reacted=read_react_s, Reposted=mediaview/mini_repost/groupCallMemberActiveIcon/offset (4,4), Forwarded=statistics/mini_stats_share, Preloader=none, RefRecipient=none); §43.10 Sizing fully resolved (summary item _height=top+font+bottom=9+font+7≈30px, iconPosition (15,7), itemPadding (44,9,17,7), GroupCallUserpics size 22 / shift 8 / stroke 4 / kMaxSmallUserpics=3; whoReadMenu scrollPadding (0,6,0,4) maxHeight 400; whenReadPadding (34,3,17,4) icon (8,0) skip 3 font 12 row 19 px; whenReadShowPadding (6,0,6,2) pill); §43.10.4 summary-row icon variants per type table; §43.10.5 userpic deferred reveal via parentMenu->st().duration ≈150 ms; §43.15 (new) style-token quick-reference table. HONEST UNRESOLVED: popupMenuExpandedSeparator body (defer §39 PopupMenu spec), Emoji::GetSizeNormal() exact runtime constant.
- §44 — complete.
- ~~§45 (11618–11697): cache atlas dims, ReactionPreview overlay, View Pack button.~~ **DONE 2026-04-19** — §45 Custom Emoji Rendering: §45.6 ReactedMenuFactory citation CORRECTED to data_custom_emoji.cpp:963-1005 (was 1075-1108); §45.8 atlas dimensions resolved (kPerRow=16, atlas = QImage(min(_frames,16)×_size, ceil(_frames/16)×_size, Format_ARGB32_Premultiplied), _size table per SizeTag: Normal 20, Large 27, Isolated 43, SetIcon 24 px; kMaxFrames=180 → 12×16 max grid; eviction state-machine Loading/Caching→cancel, Cached→state.unload()); §45.9 reaction-preview click handler (`emojiPreview` flag only changes label string lng_context_animated_emoji_preview vs lng_context_animated_reaction); §45.13 +5 source-file rows; §45.14 (new) full Reaction/Emoji Preview Overlay (View Pack) — composition (4 widgets: MediaPreviewWidget + clickable backdrop + background button + label), geometry formula (shadowExtend (10,10,10,10) + msgServicePadding (12,3,12,4) + labelWrap to half textMaxWidth → 2 lines; bgY = bodyHeight×3/4 - bgHeight/2; labelX 22, labelY 13), boxRadius 8, animation 120 ms via st::defaultToggle.duration (universalDuration), paint sequence Ui::Shadow→drawRoundedRect; Premium-sticker constants (shift 21/240, multiplier 1.49, downscale 1.25); §45.15 (new) atlas cache constants summary table. HONEST UNRESOLVED: stickerPreviewBg palette hex (palette file path differs in this fork — needs direct clone), StickerSetBox Emoji-mode geometry (defer §27), emojiUsesTextColor() flag bit position.
- ~~§46 (11368–11470): FieldHeader dims, WebPageType button bar.~~ **DONE 2026-04-19** — §46 Link Preview in Compose: §46.2 FieldHeader fully resolved (bar 49 px = historyReplyHeight chat_helpers.style:1015, fill historyComposeAreaBg, link icon at (7,7) = historyReplyIconPosition, text-block left inset 53 px = historyReplySkip, thumb 32×32 from chat.style:64 with gap 10 px = msgReplyBarSkip, text padding (6,6,11,6) = msgReplyPadding chat.style:84, title font msgNameStyle/colour historyReplyNameFg, description font messageTextStyle/colour historyComposeAreaFg, cancel IconButton 49×49 anchored top-right with ripple 40×40 inset (4,4) per chat_helpers.style:1022-1033, NO fade animation — visibility-toggled, only edit-mode photo overlay uses 160 ms defaultMessageBar.duration); §46.2 spec correction: text starts at 95 px when thumbnail present (53+32+10), not 53 px in all cases as previous draft claimed; §46.9 clarified the WebPageType "button bar" = `_openButton` rendered on messages NOT compose FieldHeader (bar 36 px = historyPageButtonHeight, padding (13,8,13,8), 1 px divider at 30% alpha, semibold centred label tinted cache->icon, IV icon prepended when page->iv set), full per-WebPageType label table (24 rows from PageToPhrase 198-262 + HasButton 264-292; Bot deliberately excluded), compose-side toggles live in DraftOptionsBox via st::settingsButton / st::settingsAttentionButtonWithIcon with icons menuIconAbove/Below/Enlarge/Shrink/DeleteAttention. HONEST UNRESOLVED: palette hex (theme-dependent, defer §57), semiboldFont exact line height (Qt platform-derived, convention 13 px), defaultTextStyle font size citation (basic.style not pulled this pass), historyPageButtonLine 30% alpha is code constant not style token.
- §47 (11726–11828): WriteRestricted dims, forbidden ripple/toast, slowmode countdown region. **AGENT CANCELED 2026-04-19 — defer to next session.**
- §48 — complete.
- §49 (12146–12233): JumpDownButton dims, sticky date header, corner buttons stack height. **AGENT CANCELED 2026-04-19 — defer to next session.**

## §51–§55 — AyuGram extensions

- §51 — complete (post-reorder).
- §52: saveForBots UI gating; deletedMark/editedMark customization paths; EditMarkBox dims; DB scope per-userId; context-menu matrix; passcode/DB-encryption link; per-chat exclusion list.
- §53: Repeat Message hint text; download path cross-ref; mid-download cancel; progress-bar error states; per-account scope note.
- §54: avatarCorners off-by-one; Material Switches tokens; context menu ordering; attach-menu-bots per-account reactivity; AI Editor popup spec; IconPicker widget; HideReactions parent behavior; channelBottomButton "with fallback"; Translation Provider Win/Linux; Peer ID default; ~~**Filters semantics (regex syntax, match scopes, case sensitivity) — totally missing**~~ **DONE 2026-04-19** — §54.16 expanded with Filter Engine Semantics subsection (ICU 78 regex, multiline always on, per-rule case insensitive, hide-only action, evaluation order including shadow-ban short-circuit, `filtersEnabledInChats` gate, cache lifecycle); ~~**Shadow Ban flow**~~ **DONE 2026-04-19** — §54.16 "Shadow Ban -- entry point, storage, semantics" subsection added (peer-list context menu at `ayu/ui/context_menu/context_menu.cpp:322`, NOT message bubble menu — also corrected misplaced entry at line 15537); plus AyuFiltersList three-mode screen, RegexEditBox edit form, PerDialogFiltersListController, ImportFiltersBox (`Ui::FillImportFiltersBox` free function with v2 JSON format) all documented end-to-end. §54.13/§54.14 renumbering; Streamer Mode now in §50 — update cross-refs.
- §55 — complete.
